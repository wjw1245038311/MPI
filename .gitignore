# Dependencies and generated builds
node_modules/
android/.gradle/
android/.idea/
android/.kotlin/
android/local.properties
android/**/build/
cloudflare-signaling/node_modules/
cloudflare-signaling/.wrangler/
out/
dist/
release/
resources/bundled/
runtime-release/
resources/runtime-manifest.json
.runtime-stage/
.legacy-resources-bundled-*/

# Local app, QA, and agent data
.claude/
.codex-remote-attachments/
.qa-appdata/
.qa-browser-data/
.qa-user-data/
# Relay runtime state (VAPID private key — never commit)
mobile/relay/data/
.tmp-*/
mpi-gates/

# Internal working docs (local only)
SUBAGENT-PLAN.md
improvement-suggestions.md
review-todo-*.md
MPI-USER-MANUAL.md

# Secrets and logs
.gh-token
.ecs-pwd
.env
.env.*
!.env.example
*.log

# Editor and OS files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local QA output
qa-*.png
nul
tmp/
# 本地临时脚本/缓存工作区（审计、基准等，不入库）
tempfile/
# Staging dir for app packages to upload/share (large zips, never commit).
# Anchored to repo root: a bare "Apps/" would case-insensitively match examples/apps/ on Windows.
/Apps/

# Generated test bundle (npm run test:tuirace)
scripts/.tui-race-bundle.cjs
android/publish/

# 新原生手机端工程（Kotlin + Compose，docs/MOBILE-NATIVE-DESIGN.md）
mobile/app/.gradle/
mobile/app/.idea/
mobile/app/.kotlin/
mobile/app/local.properties
mobile/app/**/build/
mobile/app/publish/

# 本地进程/重启日志（scripts/restart-dev.sh 输出）
logs/

# alexandria 知识引擎：索引 db 是编译产物（可丢弃重建）；toml+knowledge/ 进 git
.alexandria/index/

# 回填时的原文备份（临时，不入库）
.alexandria/knowledge/lessons/.backup-bare/
