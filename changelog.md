# MPI Changelog

MPI —— 基于 Pi coding agent 的桌面客户端。本文件记录近期各版本的改动。

**维护约定**：每次提交更新后，将改动追加到下方 `Unreleased` 小节；打包发版时把 Unreleased 内容移入新的版本小节并更新日期。每个功能/优化条目附一段独立换行的「验证方式：」，写清如何在应用里操作确认该条生效（供安装后逐条实测）。

## v0.6.9（2026-09-12）

1. **任务模式升级：行为指令 + 设计说明书（类 skill），新增调研/审查/谨慎三个内置模式**：此前任务模式只打包权限+思考等级两个环境参数，agent 的工作方式不会真正改变。现在每个模式可携带**行为指令**（短文本）和/或一份 **.md 设计说明书文件**——应用该模式的会话在每轮对话时由新的 mpi-taskmode 扩展把内容注入系统提示词（pi `before_agent_start` 事件，实时生效、切换模式无需重启进程；说明书是外部文件，直接编辑文件即更新）。新增三个内置模式（参考 AI Agent 设计模式：Tool Use / Reflection / Human-in-the-Loop）：**调研**（沙盒+中思考：广搜多源交叉验证、不写代码、结论标来源）、**审查**（只读+极高思考：不改文件、按严重度列问题、输出前自查）、**谨慎**（严格+中思考：破坏性/外发操作前先说明等确认）；长任务也预填了 Planning 风格指令（先拆子任务再逐步执行）。短任务保持零注入（默认路径无额外 token 开销）。管理弹窗表单新增「行为指令」多行输入 + 「设计说明书」文件选择（内置模式同样可改，沿用参数可编辑语义）；下拉摘要对带说明书的模式显示「含说明书」。自定义模式不限于代码场景——写文档/数据分析等都能挂自己的说明书。

   验证方式：① dev 完整重启后，⚡下拉应出现 5 个内置模式（短任务/长任务/调研/审查/谨慎）；点「审查」→ 权限变只读、思考极高。② 在会话里让 agent「列出这个项目的三个潜在问题」（审查模式下）→ 它应先通读再按严重度列问题且不改任何文件；切回短任务后行为恢复默认（指令注入随模式切换实时生效，无需重启）。③ 「管理任务模式…」编辑任一模式：填一段行为指令保存 → 应用该模式后 agent 行为体现指令内容；「设计说明书」浏览选一个 .md 文件 → 下拉摘要出现「含说明书」，直接改文件内容下一轮即生效。④ 新建自定义模式挂一份自己的说明书（如写作风格指南）→ 应用后对话按文档执行。⚠️ main+preload+renderer 改动，dev 需完整重启后生效；状态文件在 <userData>/taskmodes/。

2. **任务模式审查修复 + 新增内置「默认」模式**：⚡下拉新增首位内置**默认**（基线行为：无参数、无指令注入）——新会话与从未应用过模式的会话现在显示「默认」而非隐式回退短任务；点它 = 清除当前行为注入且不改动权限/思考。同时修复审查发现的 7 个逻辑问题：① 模式选择现在持久化（config.threadTaskModes），重启或关闭重开会话后 ⚡ 仍显示上次所选——此前会退回默认显示而 agent 继续按旧模式指令行事；② 未发送过消息的新草稿上直接选模式不再丢失行为指令、也不再弹「Thread not open」错误；③ 预览面板关闭时把弹出窗口拖回主窗口现在能正确停靠（此前静默无反应）；④ 管理弹窗里编辑/删除模式后，已应用该模式的会话同步更新（删除会清除注入并重置悬空的默认指针）；⑤ 删除会话时清理其任务模式状态文件与配置键（回收站恢复不再复活旧模式）；⑥ 草稿会话的权限切换不再往 config.json 写无效记录；⑦ 删除运行中会话先等当前轮次落定再移入回收站，避免 JSONL 以悬空工具调用结尾。

   验证方式：① dev 完整重启后 ⚡下拉首位出现「默认」（摘要「基线行为（无附加指令）」）；新开会话 pill 显示「默认」。② 给会话挂「审查」→ 完整重启 MPI → 重开该会话，pill 仍显示「审查」、agent 仍按审查协议行事。③ 新建草稿不发消息直接点「审查」再发第一条消息 → agent 按审查行为执行、无错误 toast。④ 拖出预览 tab → 关闭预览面板 → 把浮动窗口标题条拖回主窗口释放 → 自动停靠并重新打开面板。⑤ 管理弹窗改某模式的行为指令保存 → 已挂该模式的会话下一轮即生效；删一个自定义模式 → 相关会话注入立即清除、pill 回落「默认」。⚠️ main+preload+renderer 改动，dev 需完整重启后生效。

3. **健壮性小修（权限门/预览拖动/配置损坏）**：权限门扩展不再每次工具调用都重读并解析 config.json（按 mtime+大小缓存，语言切换仍即时生效）；弹出预览窗口的标题条拖动加 15s 看门狗——渲染进程若在拖动中崩溃会自动结束拖动，窗口不再粘住光标；config.json 损坏时先备份为 config.json.bak 再回退默认值（此前会被下次保存静默覆盖丢失）；设计说明书的文件选择对话框现在只列出 .md/.markdown 文件（与标签一致）；备份/恢复明确不导入任务模式（说明书路径是机器相关的绝对路径，跨机导入会悬空——自定义模式可在管理弹窗重建或从 config.json 备份手动拷贝）。

   验证方式：① 任意会话正常对话无感知变化（权限确认卡、语言显示均正常）。② 弹出预览窗口拖动标题条时强杀渲染进程不可行，可观察日志：正常拖动释放后无残留定时器；该路径主要防崩溃场景。③ 手动把 <userData>\config.json 改成非法 JSON → 重启 MPI → 设置回默认值且同目录出现 config.json.bak（内容即损坏前的文件）。④ 「管理任务模式…」→「设计说明书」浏览 → 对话框文件类型下拉为 Markdown 文档，选不到 .txt。⚠️ main+preload+renderer 改动，dev 需完整重启后生效。

4. **任务模式「默认」恒置顶 + 修复 Markdown 预览目录锚点点击不跳转**：① 内置「默认」模式现在始终排在 ⚡下拉第一位——此前在已保存过旧版模式列表（5 个）的机器上，新增的默认会被追加到末尾；normalizeTaskModes 归一化后强制置顶。② 修复 Markdown 预览里点目录链接无反应的长期 bug（如用户手册「1. 认识 MPI」）：react-markdown 会把 href 里的非 ASCII 字符百分号编码（#1-%E8%AE%A4…），而 rehype-slug 生成的标题 id 是原始中文，getElementById 永远找不到；现在查找前先 decodeURIComponent 再平滑滚动。聊天消息里的 #锚点链接同样受益。

   验证方式：① dev 完整重启后 ⚡下拉第一项即「默认」（即使 config.json 里已存过旧版 5 模式列表）。② 预览打开 resources/user-manual.md → 点目录任意条目（如「1. 认识 MPI」）→ 页面平滑滚动到对应标题；英文手册 TOC 同样可跳。⚠️ renderer 改动，dev HMR 即生效。

## v0.6.8（2026-09-12）

1. **会话内方案选择卡片（mpi_ask_choice）**：此前 agent 给出多个设计方案时只能靠你手动打字回复「选 A」。现在 agent 在正文写完方案对比后调用新的 `mpi_ask_choice` 工具，输入框上方直接弹出可点击的选项卡（与权限确认卡同一套扩展 UI 通道）——点一下即选中，选择作为工具结果回到当轮，agent 立刻按所选方案继续，全程零打字。卡片带「方案选择」角标与分支图标、不再把第一个选项硬标为推荐；关闭卡片 = 未选择，agent 会改用文字追问而不会擅自选。每个选项可带可选的细节说明（agent 传 `{label, detail}`），卡片上点「细节」展开/收起，不用翻回正文对比；卡片底部另有「其它（输入自定义方案）」——现有选项都不合适时直接打字提交，文本走同一通道作为选中结果返回给 agent。历史消息里该工具调用渲染成紧凑选择卡：问题 + 全部选项（同样可展开细节），选中项打 ✓ 高亮；选了「其它」自定义方案时在列表末尾单独一行 ✓ 显示所输内容（参考 Qoder CN IDE 的会话内选项交互）。配套：桌面端切到别的程序时弹系统通知「等待你的选择」；飞书/微信等通道会话无法点击 → 通道里发提醒并在宽限期后自动取消、agent 退回纯文本列选项；定时任务不加载该扩展，无人值守不会挂起。

   验证方式：① 在任意会话里让 agent「先出几个方案让我选」（如让它设计一个小功能并给 2-3 个实现方案）→ 正文出现方案对比后，输入框上方弹出带分支图标的「方案选择」卡，选项为按钮；点其中一个 → 卡片消失、agent 在同一轮直接按所选方案继续（无需再打字）。② 重新打开该会话回看历史 → 对应位置是紧凑选择卡：问题 + 选项列表，选中的那行有 ✓ 高亮。③ 弹出卡片后点右上角 ×（关闭）→ agent 收到「未做出选择」并用文字追问，不会擅自开工。④ 让 agent 给带细节的方案（如「每个方案附一句取舍说明再让我选」）→ 选项右侧出现「细节」胶囊按钮，点开展开说明、再点收起；点底部「其它…」→ 出现输入框，写一段自定义方案并确认提交 → agent 按所输内容继续，历史卡末尾多一行 ✓ 显示该内容。⑤ 沙盒权限下调用该工具不再先弹授权卡（直接出选择卡）。⚠️ main+preload+renderer 改动，dev 需完整重启后生效；旧会话历史中已有的 mpi_ask_choice 记录同样会渲染成选择卡。

2. **dev 一键发版改为「等 GitHub Actions」模式，不再从家里上传大文件**：此前本地打包后由脚本把 140MB+ 安装包从家庭宽带传到 GitHub（上行 ~1Mbps 要 ~20 分钟且易被中断）。现在 push tag 后 GitHub Actions 自动在自家机房打包并传到 Release 页（几分钟），脚本只轮询附件就位再校验；本地打包保留但仅用于复制 Seafile 分发副本。发版总时长从 ~25 分钟降到 5~10 分钟，家庭网络全程只出几 KB。CI 超时（默认 20 分钟）时脚本报错并提示手动重跑 `node scripts/publish-release.mjs <version>` 走本地上传兜底。

   验证方式：① 发起发版评审并确认发布 → tag push 后日志出现「已推送 v0.x.y（GitHub Actions 构建开始）」，不再有「正在上传 MPI-Setup-x.y.z.exe」进度行；等待期间 dev 窗口长任务监控圆环按钮显示「正在等待 GitHub Actions 构建上传…」条目。② CI 完成后脚本输出附件校验全 ✓ + Seafile 复制完成。③ 手动跑 `node scripts/publish-release.mjs <已有版本> --wait-ci` → 立即通过「GitHub Actions 附件已全部就位」（幂等）。

3. **任务模式切换（权限按钮左边的预设 pill）**：此前处理不同类型任务要手动逐个调「权限级别」和「思考等级」。现在输入框底栏新增任务模式预设（位于权限按钮左侧，⚡图标）：内置**短任务**（沙盒+低思考）/**长任务**（沙盒+高思考），点击即批量应用、长任务模式下按钮蓝色高亮；下拉底部「管理任务模式…」可新建自定义模式（名称 + 权限级别 + 思考等级，每项可选「不改变」保持当前值），内置模式参数可改但不可删除。模型选择保持独立、不受任务模式影响（成本敏感项不被预设静默切换）。新会话默认显示短任务；手动单独调参不影响模式列表。

   验证方式：① 任意会话点权限按钮左侧的 ⚡「短任务 ▾」→ 下拉列出短/长任务（带参数说明）+「管理任务模式…」；点「长任务」→ 思考等级变为高（模型弹层内可见）、若权限有变化会弹对应 toast、模式按钮变蓝色。② 「管理任务模式…」→ 新建：名称「代码评审」、权限只读、思考高 → 保存后下拉出现该模式，点击应用 → 权限按钮变「只读」。③ 管理弹窗里编辑长任务的参数（如权限改完全权限）→ 保存后再点长任务即按新参数切换；内置模式的删除按钮置灰。⚠️ main+renderer 改动，dev 需完整重启后生效。

## v0.6.7（2026-09-12）

1. **长任务监控（输入框底栏圆环按钮 + 详情弹层）**：此前扩展包安装/更新/移除、Pi CLI 系统更新、Pi 核心下载、应用更新下载、dev 发版上传等长操作没有任何进度反馈，只能盯着日志窗口干等。现在任一操作运行超过 10s，输入框底栏（上下文用量圆环旁）出现小圆环按钮——有总大小的传输显示百分比进度、否则转圈；颜色随最长任务时长升级（60s/3min 两档告警色），多任务时带数量角标。点开弹层列出全部运行中的长任务，两类来源：① Agent 工具调用（bash 等）——显示命令与输出末几行，可停止该会话当前轮次；② MPI 传输——扩展包 npm 操作（实时显示正在拉取的包、可取消）、Pi CLI 系统更新、Pi 核心下载（进度条+速度、可取消）、应用更新下载（进度条+速度）、dev 发版 GitHub 上传（JSONL 桥文件→主进程 tail，仅 dev 模式启用）。

   验证方式：①「扩展功能」面板安装任一扩展包并等 >10s → 底栏出现小圆环按钮；点开有任务卡显示 npm 输出末行，点停止可取消该安装。② 设置→关于 MPI→检查 Pi 核心更新（或应用更新）下载，>10s 后点圆环看进度条+速度。③ dev 模式：发起发版评审并确认发布，exe 上传阶段圆环显示「正在上传 MPI-Setup-x.y.z.exe」百分比/速度；流水线中断后条目自动清除（30s 心跳超时）。⚠️ main+preload 改动，dev 需完整重启后生效。

## v0.6.6（2026-09-11）

1. **飞书扫码接入支持选择已有应用（对齐 Hermes）**：此前扫码确认页只能新建应用（SDK `createOnly=true`），企业里已建好的机器人无法复用，反复扫码会越积越多。现在去掉该限制：手机确认页同时提供「已有应用」与「立即创建」两个入口——选已有应用时页面展示本次将追加的权限/事件差异并需再次授权，确认后同样直接返回 App ID/Secret 写入本机；新建流程不变（预配 im 三权限 + im.message.receive_v1）。面板文案同步更新（按钮改「📱 扫码选择或创建应用」，已绑定时为「重新扫码（更换 / 新建应用）」）。

   验证方式：① 消息接入 → 飞书 →「📱 扫码选择或创建应用」→ 手机飞书扫码，确认页底部出现「已有应用 / 立即创建」两个按钮（与 Hermes Agent 一致）。② 点「已有应用」选一个已建好的机器人并同意 → 桌面端 toast「接入成功，凭证已保存（cli_xxx）」，表单 App ID 自动填入该应用。③ 走「立即创建」仍按原流程新建「MPI 智能助手」。⚠️ main 进程改动，dev 需完整重启后生效。

## v0.6.5（2026-09-11）

1. **dev 一键发版改为两段式「发版评审」流程**：原来点「发布新版本」直接执行流水线（工作区不干净还会置灰阻断，太麻烦）。现在点击后打开一个以新版本号命名的评审会话（如 v0.6.5）：展示 changelog Unreleased 待发布内容 + 工作区状态；你在对话中明确确认后，才由该会话的 agent 运行 scripts/dev-release.mjs 执行完整流水线——未提交改动自动 git stash -u 暂存（含 untracked，构建仍只基于已提交代码），流水线结束后自动 pop 恢复；若恢复冲突则暂存条目保留、可在新会话里 git stash list/pop 处理。提出修改意见则先改再等确认；token 缺失时会在预检阶段报错且不留 stash。

   验证方式：① 设置 → 关于 MPI → dev 面板点「发起发版评审」→ 打开标题为 v0.6.x 的新会话，agent 给出本次发版内容摘要。② 未确认前不执行任何 git/npm 命令；回复「确认发布」后脚本运行、输出显示在对话中。③ 发版完成后工作区未提交改动自动恢复（git status 回到原样）。

## v0.6.4（2026-09-11）

1. **修复 Pi 核心更新在已是最新版本时仍提示「可更新」**：原检查只比较 MPI 管理的运行时副本（userData/内置），而 dev 模式或自定义 pi 路径下该副本不存在（或落后于实际使用的 pi），却总是报有更新；界面显示的「当前版本」又回退到实际运行的 Pi 版本——于是出现当前 = 最新、却仍挂「可更新」标签。现在与诊断/更新按钮走同一套运行时解析：dev 模式比较系统 pi、自定义路径比较该路径的版本，managed 副本只在它才是实际运行时才参与比较；完全无法确定当前版本时明确报错而不是猜。

   验证方式：① dev 模式（npm run dev）且系统 pi 已是最新 → 设置 → 关于 MPI →「更新 Pi 核心」卡片显示当前 = 最新、无「可更新」标签，按钮为「检查并更新 Pi」。② 点它 → toast「Pi 已是最新版本。」。③ 已安装版本里内置运行时确实落后时（如 0.84.1 vs 最新 0.85.1）仍正确提示可更新，点击完成升级后标签消失。


2. **应用更新「检查最新版本」补结果反馈**：此前点检查后若无新版本（或 dev 模式直接跳过检查）界面无任何变化。现在每次点击都有 toast：发现新版 →「发现新版本 vX.Y.Z」；已是最新 →「MPI 已是最新版本（vX）」；失败 → 错误信息；dev 模式 → 提示不支持应用内更新检查，且卡片下方常驻一行说明。

   验证方式：① 已安装版本点「检查最新版本」→ GitHub 无更新时 toast「MPI 已是最新版本（v0.6.x）」；有更新的 Release 时 toast「发现新版本 vX.Y.Z」并出现下载按钮。② dev 模式点击 → toast「开发模式不支持应用内更新检查（仅已安装版本可用）」，卡片下方常驻说明行。


3. **dev 一键发版日志支持独立窗口查看**：面板内联日志框只有约 15 行高（260px），构建/发布几百行输出看不全。按钮行新增「独立窗口查看完整日志」（有日志或发版进行中时出现）——点击弹出独立日志窗口（780×540、可缩放）：打开即回放本次会话全部历史（最多 5000 行），运行中实时追加；自动跟随滚动（手动上翻不打扰，回到底部附近恢复跟随）；「复制全部」一键拷贝全文。

   验证方式：① dev 模式设置 → 关于 MPI → dev 面板点「发布新版本」后按钮行出现新按钮；点击 → 弹出标题为「MPI dev 发版日志」的独立窗口，显示此前全部日志并实时更新。② 上翻查看旧输出不会被拉回底部；滚回底部附近恢复自动跟随。③ 「复制全部」拷贝完整日志（按钮短暂显示 ✓ 已复制）。④ 关闭窗口再打开仍回放本次会话历史。


4. **更新日志改为独立窗口打开**：原「查看更新日志」是应用内弹窗，从设置里打开时被父容器困住（卡片 hover 动画会改变 fixed 元素的定位基准），表现为弹窗卡在设置窗口里、拖不出来、关不掉、还会闪动。现在点「查看更新日志」直接弹出独立原生窗口（860×640、可缩放）：显示随包内置的全部更新记录 + 安装校验方法，跟随主题/语言；重复点击聚焦已有窗口不重复打开。

   验证方式：① 设置 → 关于 MPI（或帮助 → 关于 MPI）点「查看更新日志」→ 弹出标题为「MPI 更新日志」的独立窗口，内容完整可滚动、右上角有当前版本徽标。② 窗口可正常拖动/缩放/关闭，关闭不影响设置页；不再出现卡住或闪动。③ 再次点击重新打开（或聚焦）同一窗口。

5. **dev 一键发版不再要求工作区干净**：原来有未提交改动时「发布新版本」按钮置灰、必须先手动 commit/stash 才能发版，太麻烦。现在按钮随时可点：预检阶段自动 git stash -u 暂存全部未提交改动（含 untracked），构建/发布仍只基于已提交代码；流水线结束（成功/失败/取消）后自动 pop 恢复。若 WIP 恰好与本次 bump 改了同一区域导致恢复冲突，暂存条目保留并在日志中提示——在新会话里 git stash list / git stash pop 处理即可。

   验证方式：① 工作区有未提交文件时点「发布新版本」→ 不再置灰；日志 [1/5] 列出文件后显示「已自动暂存 N 个未提交文件」。② 发版完成后 git status 恢复原样（WIP 文件全部回来）。③ 中途取消也能自动恢复。④ token 缺失时点击会在预检阶段报错，且不会留下 stash。

## v0.6.3（2026-09-11）

1. **设置「关于 MPI」新增 dev 一键发版面板（仅开发模式可见）**：一键完成 bump patch → changelog Unreleased 改名 → commit → push origin → npm run dist → publish-release.mjs（tag/push/GitHub Release/附件上传），全程日志逐行流式显示在面板内、可随时取消；预检要求工作区干净且 GitHub token 就绪（env GITHUB_TOKEN 或仓库根 .gh-token，token 只在 main 进程读取、不传 renderer），有未提交改动会中止而不会自动提交别人的 WIP。

   验证方式：① dev 模式（npm run dev）下设置 → 关于 MPI 底部出现第三张卡片「dev 一键发版」；安装包版本里该卡片不显示。② 卡片展示当前版本→下一版本、工作区状态、token 状态；有未提交改动时列出文件并禁用发布按钮。③ 干净工作区 + token 就绪时点「发布新版本」→ 日志逐行流式输出（预检 → bump → commit → push → 构建 → 发布），完成后 toast「vX.Y.Z 发版完成」，GitHub 上生成 tag/Release/安装包附件。④ 进行中点「取消」→ 终止当前步骤；若版本提交已做出会给出 git reset 撤销提示。

## v0.6.2（2026-09-11）

1. **设置新增「扩展选模」开关（默认开）——扩展要模型不再弹窗**：扩展需要选择模型时，MPI 自动使用当前会话的模型、不再弹出询问。对 pi-web-access 联网搜索的具体效果：① 每次搜索不再打开浏览器整理窗口（`workflow=auto-summary`），直接返回 AI 生成的摘要；② 摘要模型实时跟随当前会话的模型（每轮开始同步，手动切模 / Auto 模式切换后同样生效）；③ 摘要生成超时从默认 30s 放宽到 5 分钟，本地/慢速模型不会退化成确定性兜底摘要。实现：新增 `src/main/web-search-config.ts` 管理 `~/.pi/web-search.json`（与终端 pi 共享）的 workflow / summaryModel / summaryGenerationDeadlineMs 三个键——读改写保留 API Key 等其它全部键；首次触碰把原值备份到 `<userData>/web-search-orig.json`，关闭开关时精确还原（MPI 新建的文件会删掉、用户后加的键保留）；文件损坏绝不覆盖。另加通用拦截：任何扩展的 `ui.select` 选项若全是 provider/model 形式，自动用当前会话模型应答（定时任务无人值守同样生效），后续其它插件弹窗选模也按此处理。新增配置项 `extAutoPickModel`（缺省=开）+ 设置→通用「扩展选模」行；测试 `test:websearch`。注意：该文件与终端 pi 共享，开启后终端里的联网搜索也会默认走 auto-summary（单次调用仍可传 workflow 覆盖）。

   验证方式：① 设置 → 通用 → 「扩展选模」行存在且默认勾选。② 会话里让智能体联网搜索（如“搜一下 xxx”）→ 不再弹出浏览器窗口，工具结果直接带 AI 摘要。③ 用文本编辑器打开 `~/.pi/web-search.json`：workflow="auto-summary"、summaryModel=当前会话模型（provider/model-id）、你原有的其它键原样保留。④ 把会话切到另一个模型再搜一次 → summaryModel 随之更新。⑤ 取消勾选「扩展选模」保存 → web-search.json 恢复为 MPI 触碰前的状态（原本没有该文件则被删除）。

2. **输入框发送/停止图标换新**：发送按钮（含流式中的「排队后续」）由细线向上箭头改为描边纸飞机（Lucide Send 风格），小尺寸下更精致；停止按钮的白色方块从 14px 放大到 16px，不再糊成墨点。纯 renderer 改动。

   验证方式：输入框右下角发送按钮显示为纸飞机图标；让智能体开始生成后，右侧出现两个按钮——纸飞机（排队后续）+ 红色圆钮停止，停止钮内白色方块比之前明显更大更清晰。

3. **会话内搜索（Ctrl+F）升级为浏览器「查找」体验**：① 输入防抖 200ms——不再每敲一个键就全量扫描整个会话，停顿后才搜一次（清空立即生效），长会话下开销大幅下降；② 所有命中位置在消息正文里逐处高亮（`<mark>`，代码块、加粗/列表内同样生效）——不再是「只给一个区块、还要自己在里面找」；未命中的消息照旧调暗，当前消息保留描边+闪烁定位；③ 搜索语料与渲染内容完全对齐：assistant 按每个文本块独立检索（不再用 \n 拼接多块）、用户消息只搜可见正文（/skill 内容与 HTML 引用卡片不参与），「i/N」计数与实际看到的高亮一致。实现：新增 `lib/search-mark.tsx`（DOM 级注入/移除高亮，不走 React 渲染——查询变化绝不触发 markdown 重解析）；Chat.tsx 防抖 + 逐消息标记（流式中的消息跳过内联高亮，避免与实时刷新互相干扰）。测试：`test:chatsearch` 更新适配新语料规则；新增 `test:searchmark`（esbuild 打包后在项目自带 Electron/Chromium 里跑真实 DOM 断言，含 react-markdown + highlight.js 集成用例）。

   验证方式：① 会话里 Ctrl+F 输入一个出现多次的词 → 停顿约 0.2s 后全会话所有出现处被高亮（代码块内也算），未命中消息调暗，计数显示 i/N。② Enter/Shift+Enter 或 ↑↓ 在命中间移动——视图滚动到对应消息并描边闪烁；Esc 关闭后所有高亮消失、消息恢复原样。③ 搜一个只出现在 /skill 内容或 HTML 引用卡片里的词 → 无匹配（这些部分不作为正文渲染）。④ 长会话里连续快速输入 → 只在停顿后才发生一次扫描，不卡顿。

4. **移除标题栏状态显示**：去掉标题栏右侧原先的「会话名 · 当前模型 / Pi 已就绪」小字（纯信息展示、无功能影响），标题栏更简洁。纯 renderer 改动。

   验证方式：窗口右上角设置按钮左侧不再出现灰色状态文字；其余功能不受影响。

5. **LICENSE 补充 Pi Studio 来源声明**：MIT 许可头部加一行 “Portions derived from Pi Studio”（用户无可见变化）。

   验证方式：无用户可见变化，可在 GitHub 仓库确认。

## v0.6.1（2026-09-11）

1. **首个发布在 MPI 自有 GitHub 仓库的正式版本**：发布到 `wjw1245038311/MPI` 的 Releases——应用内「帮助 → 关于 MPI」→「检查最新版本」从此真正可用：从本仓库检查更新，后续大版本都发在这里，可在应用内直接下载安装包。（自更新功能 v0.6.0 已启用并指向本仓库，但当时 GitHub 上还没有任何 release；本版本补齐。）

   验证方式：帮助 → 关于 MPI → 点「检查最新版本」→ 已是 0.6.1 时提示当前为最新；发布更高版本后显示可更新并可应用内下载安装包。

2. **仓库清理（用户无可见变化）**：LICENSE 改为 MPI 自有版权（MIT）、README 截图全部换成 MPI 界面图、内部工作文档移出仓库跟踪（.gitignore 过滤）。

   验证方式：无用户可见变化，可在 GitHub 仓库确认。

## v0.6.0（2026-09-11）

1. **设置「模型与提供商」改为两段式布局（预设供应商 + 我的供应商）**：原来一长串平铺的供应商大卡片拆成上下两段。上段「预设供应商」为卡片网格：标题行带搜索框（按名称过滤）和「刷新预设」按钮（重读 models.json，更新各平台配置状态）；内置 阿里云百炼（API 按量付费 + Coding Plan）/ 智谱 / DeepSeek / Kimi·Moonshot / Gemini / GPT·OpenAI 七张预设卡（预填地址、API 类型与首个模型，端点与默认模型对齐 pi 运行时内置目录），另有「自定义配置」「本地部署」两个虚线入口（LM Studio、Ollama 等自托管服务从这里进）。点击未配置的预设卡 → 打开预填好的新增表单，补上 API Key 即可添加；已配置的预设卡带强调色描边，若它还是默认提供商则显示绿色「当前」徽标——此时点击直接展开下段对应供应商的完整编辑器（按 provider id 或地址 host 匹配）。下段「我的供应商」为紧凑卡片网格（名称 + 模型数 + 地址 + 「当前」徽标），点击在下方展开完整编辑区（复用原供应商卡：基础地址 / API 类型 / Key / 请求头 / compat JSON / 逐模型设置，重命名、删除都在这里）。新增时**模型 ID 可留空**（本地部署常用，稍后在编辑器里补）；添加成功后自动展开新供应商的编辑区。内置使用手册第 4.1 节同步更新。

   验证方式：① 设置 →「模型与提供商」→ 上段显示「预设供应商」标题 + 右侧搜索框/刷新预设按钮，下方为卡片网格（2 张虚线入口卡 + 7 张平台卡：阿里云百炼×2、智谱、DeepSeek、Kimi / Moonshot、Gemini、GPT / OpenAI）；在搜索框输入关键字可按名称过滤。② 点一张未配置的预设卡（如 DeepSeek）→ 出现预填好地址/API 类型/模型 ID 的新增表单 → 填 API Key 后「添加供应商」→ 下段出现该供应商卡片且自动展开其编辑区；点「保存模型配置」后可在输入框模型选择器里看到。③ 点一张已配置的预设卡（如 DeepSeek，按 provider id 或地址 host 匹配）→ 直接在下段网格下方展开对应供应商的完整编辑器；再点一次该卡片或点「收起」可折叠。④ 默认提供商（思考默认值里的默认提供商）在上、下两段都显示绿色「当前」徽标。⑤ 新增表单里模型 ID 留空也能提交成功，新供应商暂无模型，可在编辑区「添加模型」补上。

2. **设置新增「用户画像」分栏**：在自由文本里写自我介绍与偏好（≤4000 字，如职业/技术栈/希望回复怎么组织），MPI 通过 pi 的 `--append-system-prompt` 把它附加到每个会话的系统提示词里——普通会话与定时任务都生效；留空保存 = 关闭注入。对新会话生效，已打开的会话重连后生效（保存时自动重建热备进程）。存于应用配置目录 `config.json` 的 `userProfile` 字段，不影响终端 pi。

   验证方式：① 设置 →「用户画像」→ 输入文字点「保存用户画像」→ 按钮变「已保存 ✓」+ toast；未保存时标签页有小圆点，关闭设置会提示放弃。② 开一个**新**会话问“你知道我是什么人吗？”→ AI 能复述画像内容（职业/偏好等）。③ 清空文字再保存 → 新会话中 AI 不再知道画像内容。④ 终端 pi 行为不变。

3. **设置新增「备份与恢复」分栏**：两个独立备份对象，各自导出/导入。**应用设置**：把 `config.json`（主题、语言、置顶、头像、用户画像、定时任务、远程信令等）导出为单个 JSON；导入先显示可识别项数并要求确认，确认后只覆盖备份包含的字段（其余保持不变），机器相关项（pi 路径、窗口位置）永不恢复。**会话**：按项目勾选（全选/清空 + 每行会话数与大小），原始 JSONL 打包为 zip（保留项目目录结构）；导入先提示「N 新 / M 已存在」，默认只导新的跳过已有文件，也可选「覆盖全部」以备份为准恢复。模型提供商存于 `~/.pi/agent`（与终端 pi 共享），不在备份范围。新增 IPC `backup:listSessions/exportConfig/pickConfigImport/exportSessions/pickSessionImport/importSessions` + preload `window.pi.backup`；导入会话后自动刷新导航栏。

   验证方式：① 设置 →「备份与恢复」→ 上段点「导出配置」→ 保存对话框默认名 `mpi-config-backup-<日期>.json`（文档目录）→ toast 显示导出路径。② 「导入配置」选刚导出的文件 → 弹窗列出可识别项数并确认 → 改一项设置（如主题色）再导入旧备份 → 该项被覆盖回备份值、其余不变；toast「已导入 N 项设置」。③ 下段会话列表显示各项目（名称/会话数/大小，默认全选），取消勾选某项目后点「导出所选会话」→ zip 保存对话框 → toast 含会话数与路径。④ 用文件管理器打开该 zip：内含 `manifest.json` + 各项目的 `<目录>/<uuid>.jsonl`；删掉其中一个会话文件再「导入会话」选该 zip → 弹窗提示「1 个新会话，N-1 个已存在」→ 「导入新会话」只补回被删的那个（导航栏自动出现）；再导入一次 → 全部跳过。⑤ 把某会话改过内容后再导入并选「覆盖全部」→ 该会话恢复为备份时的内容。

4. **导航栏新增「消息接入」模块（飞书渠道）**：在飞书里给机器人发消息即可驱动 MPI——私聊或群里 @它，消息路由到绑定项目文件夹下名为「飞书接入」的专属会话执行；回复先显示「🤔 正在处理」占位，随后随 agent 输出流式刷新（约 2.5s 节流），完成后定稿（过长截断，完整结果在 MPI 里看）。命令：`/new` 新建会话、`/help` 帮助。走飞书开放平台 **WebSocket 长连接模式**（无需公网 IP / 内网穿透）；同一时间只处理一条消息，按 message_id 去重防重复推送；渠道仅在 MPI 运行时在线。App Secret 只存本机 `config.json`，永不回传渲染端（`app:getConfig` 已屏蔽）。新增 `src/main/messaging/` 模块（复用 remoteBackend 的 prompt/subscribeThread，与手机远程控制同一套后端）、IPC `messaging:getState/setConfig` + preload `window.pi.messaging`、`config.feishuChannel`；面板内置飞书建应用指南（权限 scope / 事件订阅步骤）；手册 zh/en §17（原 17–19 顺延为 18–20）；测试 `test:messaging`。当前仅支持飞书，其他平台后续再议。排障修复：绑定目录若还不是 MPI 项目（无会话且未置顶）会自动置顶到导航栏，否则建会话会 NOT_FOUND；占位回复提前到解析会话之前，任何失败都会在飞书回错误消息而非静默。另：个人版租户（非企业）不支持长连接事件推送——能连上但永远收不到消息、后台无发布入口，需先免费创建自己的企业再建应用（指南已注明）。修复事件订阅 kind 匹配错误（remotePublish 以 pi 原始事件类型作 kind，agent_settled 未被识别导致任务挂起至超时）；message_end 带 stopReason=error 时在回复中追加错误说明。修复「吞消息」：真根因是 finally 里 ref.cancelled=true 使最终稿写入成为死代码——回复永远停在最后一个流式快照（尾巴丢失）；改为仅 stop() 置 cancelled，正常完成时最终稿必达。配套加固：所有对 ack 消息的写入走串行化管道防乱序覆盖、最终稿带 3 次重试且失败时保底发新消息、prompt 抛错时在飞书回错误而非卡住占位。

   验证方式：① 按面板内指南创建飞书自建应用（机器人能力 + im:message.p2p_msg:readonly / im:message.group_at_msg:readonly / im:message:send_as_bot 三个权限 + 长连接模式订阅 im.message.receive_v1）并发布。② 导航栏 →「消息接入」→ 填 App ID/Secret、选绑定项目（默认沙盒权限）→「保存配置」→ 几秒内状态灯变绿（App ID 显示为打码形式）。③ 飞书私聊机器人发文本 → 先收到「🤔 正在处理…」回复，随后该消息随 agent 输出实时刷新、完成后定稿；MPI 绑定项目下出现/复用名为「飞书接入」的会话且内容一致。④ 群里 @机器人 发文本 → 同样被处理；不 @它的群消息不响应。⑤ 发 `/new` → 收到新建确认，后续消息进入新会话；发 `/help` → 显示命令帮助。⑥ 一条还在处理中时再发一条 → 收到「上一条还在处理中」提示（不会并发执行）。⑦ 关闭「启用飞书接入」保存 → 状态灯变灰、连接断开，飞书侧不再响应。

5. **导航栏新增「待办任务」模块（v0.5 最后一个模块）**：导航栏方框对勾图标打开待办面板——待办**按项目记录**，支持截止日期（可精确到分钟）、备注、文件/图片附件与智能日期解析；智能体也可在对话中直接添加。面板布局：左列为项目范围列表（全部项目 + 各项目未完成数），右列顶部为快速添加输入框（「全部项目」时先用下拉选目标项目；文字里可直接写日期——`明天发周报` → 标题「发周报」、截止明天，也识别 `周五`、`9月30日`、`12-5` 等写法；**时间**同样可写在文字里——`明天9点开会` → 明天 09:00，支持 `下午3点半`、`14:30`、`9am/3pm` 等写法，只写时间不写日期默认今天），下方智能分栏 chips（全部 / 今天（含已过期标红）/ 明天 / 本周 / 稍后 / 无日期 / 已完成，各带数量），列表行 = 复选框 + 标题（+备注预览）+ 截止日期标签（过期红色，带时间时悬停显示完整截止时间；有附件的行显示回形针徽标）+ 项目名标签（全部项目视图下）；点行展开行内编辑器（标题 / 备注 / 日期选择器 + **时间输入**（选了日期才出现）+ 清除 / **附件区**（描述下方虚线框，飞书多维表格式）：左侧常驻 ＋ 方块——**只有点它才弹系统文件框**（空白处不可点）；重复添加同一文件（再次选择/粘贴/拖入同名同大小的文件）会自动去重，只保留一份并提示「已跳过重复附件」。支持把文件/图片直接拖进编辑器，或点击虚线框内空白处后按 `Ctrl+V` 粘贴（拖入时虚线框高亮提示）；每条最多 10 个、单个 ≤50 MB；附件卡片排在 ＋ 右侧——图片缩略图（**点击打开全屏预览浮层**，右上角放大/缩小、Esc 关闭）、其他文件显示文件名与大小（点击用默认程序打开），悬停出现 × / 保存·删除，删除不弹确认）。「已完成」分栏底部有「清空已完成」。智能体集成：新增 `mpi_todo_add` / `mpi_todo_list` 工具（桥接扩展随主进程打包、运行时写入 `<userData>/mpi-todo.ts` 并经 `pi --extension` 加载，与权限门同一模式；pi-bridge spawn 时注入 MPI_TODO_FILE/MPI_TODO_INBOX_DIR/MPI_TODO_SESSION_FILE）——agent 写入走 `<userData>/todos-inbox/` 收件箱（每条一个 JSON 文件），主进程 fs.watch + 轮询兑底读取、按 id 去重入库，面板自动刷新；扩展不直接写 todos.json，主进程保持唯一写者。智能体添加的行带 **AI** 徽标，点击跳转到来源会话。存储：新增 `src/main/todo-store.ts`（JSON 数组 `<userData>/todos.json`，原子写 tmp+rename、300ms 合并 flush、退出前强制落盘；附件以副本形式默认存于 `<userData>/todo-attachments/`（设置→通用可更改位置，已有附件保留在原位置），删除待办时一并清理）；IPC `todo:list/add/update/toggle/delete/clearCompleted/addAttachments/removeAttachment/openAttachment` + preload `window.pi.todo`（附件走 ArrayBuffer 跨进程传输，图片经 `todoatt://` 自定义协议加载缩略图）；渲染端纯函数 `lib/quick-add-date.ts`（智能日期+时间解析）与 `lib/todo-sections.ts`（分栏归类 / 排序）。手册 zh/en 新增 §11「待办任务」（原 11–20 顺延为 12–21，§3 快捷入口同步改为四个按钮）；测试 `test:todos`。

   验证方式：① 导航栏 →「待办任务」→ 面板打开；顶部输入框输 `明天发周报` 回车 → 出现标题「发周报」、截止标签「明天」的行，且计入「今天/明天」分栏数量（明天）；再输 `写文档`（无日期）→ 归入「无日期」。② 点该行展开行内编辑器：改备注、选/清除截止日期后保存生效；点删除直接移除不弹确认。③ 左列选某个具体项目 → 只显示该项目的待办，新添加自动归属该项目；切回「全部项目」→ 各行带项目名标签。④ 勾选某条待办的复选框 → 移入「已完成」（标题划线）；在「已完成」分栏点「清空已完成」→ 当前范围内已完成项全部删除。⑤ 任一会话里让智能体添加一条待办（如“帮我加个待办：xxx，明天截止”）→ 面板自动出现该行且带 AI 徽标；点徽标跳转到该来源会话。⑥ 完全退出 MPI 再打开 → 所有待办仍在（存于 `%APPDATA%\MPI\todos.json`）。⑦ 快速添加输 `明天9点开会` 回车 → 截止标签显示「明天」，悬停提示含 09:00；再输 `14:30交报告`（无日期）→ 归入「今天」且带时间。⑧ 展开某条待办：选一个截止日期后出现时间输入框，填 `15:30` 保存 → 行上截止标签悬停显示完整时间；在描述下方的虚线附件区点 ＋ 方块（或直接复制/拖入）选一张图片 + 一个文本文件 → 图片显示缩略图、**点击缩略图打开全屏预览浮层**（右上角放大/缩小可用、Esc 关闭）、文本文件显示文件名与大小，点文件名用默认程序打开；同一张图片再粘贴一次 → 只保留一份且出现「已跳过重复附件」提示；悬停卡片点 × 可移除；删除该待办后附件文件被清理。⑨ 设置→通用更改「待办附件位置」后再添加附件 → 新文件存入所选目录，旧附件仍正常显示/打开。

6. **设置新增「数据存储」分栏（更改会话/待办数据位置 + 下次启动自动迁移）**：两个独立存储位置可改到本机任意目录——① 会话存储位置（所有 pi 会话记录 .jsonl，默认 `~/.pi/agent/sessions`）；② 待办数据位置（`todos.json` + 附件 + 智能体收件箱，默认应用配置目录）。点「更改…」选新目录后只记录意图、该行下方出现橙色提示；**完全退出 MPI 重启后**在窗口打开前自动把全部文件移到新位置：会话文件平铺进自定义目录（pi 的 sessionDir 布局不按项目嵌套），恢复默认时按各文件头 cwd 重新归入 `sessions/<项目目录>/`；设置里指向旧会话的路径引用（权限记录、草稿、回收站索引、AI 待办的来源会话链接）自动同步更新。安全：目标已有 todos.json 时**不覆盖**——该项保持 pending 下次启动重试（其余文件正常迁移），失败项同样保留重试；旧位置附件迁移后仍可正常打开/删除。改会话位置会同步 `~/.pi/agent/settings.json` 的 `sessionDir`，终端 pi 跟随（恢复默认则移除该键）。自定义位置时每行显示「恢复默认」。新增 `src/main/data-migration.ts` + IPC `data-migration:status/preview/set-sessions-dir/set-todos-dir` + preload `window.pi.dataMigration`；config.json 新增 `sessionStorageDir` / `todoDataDir` / `pendingDataMigration`。手册 zh/en §16.5（原 16.5–16.7 顺延为 16.6–16.8）；测试 `test:migration`。

   验证方式：① 设置 →「数据存储」→ 两行显示当前默认路径；会话行点「更改…」选一个空目录 D1 → toast「已设置会话存储位置，下次启动时迁移 N 个文件」、该行下方出现提示。② 完全退出 MPI（托盘右键退出）再重启 → 导航栏所有会话仍在；用文件管理器打开 D1：.jsonl 直接平铺其中；设置页不再显示待迁移提示、出现「恢复默认」按钮。③ 点「恢复默认」→ toast 显示将迁移数量 → 再次完全退出并重启 → 会话回到 `~/.pi/agent/sessions/<项目目录>/`（按项目重新归位）。④ 待办数据位置改到 D2，添加一条带附件的待办 → 完全退出再启动 → 待办仍在、附件可正常打开；文件管理器里 D2 下出现 todos.json + todo-attachments。⑤ 在目标目录手动放一个含内容的 todos.json 后再更改位置 → 重启后该项未被覆盖且保持 pending（控制台 `[migration]` 有错误记录），其余文件正常迁移。

7. **「消息接入」面板改为分栏布局（通道列表 + 详情）**：左侧新增「通道」列——飞书行带实时状态灯与状态文案，微信行置灰并标注「即将支持」（不可点）；右侧为所选通道的配置表单（飞书表单内容不变）。后续新渠道只需在左列加一项并实现对应表单。手册 zh/en §18.1 同步。

   验证方式：① 导航栏 →「消息接入」→ 左侧出现「通道」列表：飞书行状态灯随连接状态变色（灰=未启用/橙闪=连接中/绿=已连接/红=失败），下方小字为对应文案；微信行置灰、副标题「即将支持」、点击无反应。② 点飞书行 → 右侧显示原有配置表单，保存/指南等功能不变。③ 窗口拉窄时右栏独立滚动，左列固定不滚。

8. **「消息接入」飞书渠道支持扫码一键创建应用**：面板新增快速接入卡——点「📱 扫码创建应用」→ 手机飞书扫码并在确认页同意 → 自动在你的企业下创建机器人应用（名「MPI 智能助手」，预配 im:message.p2p_msg:readonly / im:message.group_at_msg:readonly / im:message:send_as_bot 权限与 im.message.receive_v1 事件订阅），App ID/Secret 直接返回本机主进程并写入 config.json——**App Secret 全程不跨 IPC 到渲染端**（比手动填写更严格）。走官方 node-sdk `registerApp`（OAuth 2.0 Device Flow / RFC 8628，无需公网 IP、无中转服务器）；二维码带倒计时，可取消/重新生成；成功后若渠道已启用且已绑定项目则自动用新凭证重连。新增 IPC `messaging:startAppRegistration/cancelAppRegistration` + 事件 `pi:messagingRegistration`；手册 zh/en §18.1 同步（手动方式保留为兑底）。

   验证方式：① 导航栏 →「消息接入」→ 飞书 → 点快速接入卡「📱 扫码创建应用」→ 出现二维码与倒计时。② 手机飞书扫码并在确认页同意 → toast「应用创建成功，凭证已保存（cli_xxx）」、App ID 自动填入、Secret 显示已配置；若已选绑定项目并启用，几秒内状态灯变绿。③ 在飞书里给新机器人发文本 → 正常回复链路（占位→流式→定稿）。④ 扫码中点「取消」→ 轮询停止；等倒计时走完 → 显示已过期、可重新生成。

9. **「消息接入」飞书渠道支持列出与切换会话**：新增两个命令——`/list`（或「列表」）列出绑定项目最近 10 条会话（➜ 标记当前），`/use <序号>` 切换到对应会话（也支持直接填会话 ID）；切换后后续消息路由到该会话。活跃会话持久化到 `config.feishuChannel.activeThreadId`，重启后自动恢复（若已不存在则回落到「飞书接入」专属会话）；`/new` 同步持久化新会话。手册 zh/en §18.3 命令列表同步。

   验证方式：① 飞书发 /list → 返回编号会话列表（当前带 ➜）。② /use 2 → 「✅ 已切换到会话」，后续消息进入该会话。③ 重启 MPI 再发消息 → 仍进上次切换的会话。④ /use 99 或无效 ID → 友好报错提示。

10. **「消息接入」飞书渠道支持一键启用官方飞书 MCP**：表单底部新增「🔌 启用飞书 MCP」按钮——把官方 `@larksuiteoapi/lark-mcp`（npx stdio、应用身份模式）以当前渠道凭证写入 `<agentDir>/mcp.json`（条目名 lark-mcp），会话中的智能体即可直接调用飞书 API（默认工具集：消息 / 文档 / 日历 / 多维表等）。对新会话生效；可在「扩展功能 → 我的 MCP」中停用或删除。按钮状态实时反映 mcp.json（已启用显示 ✓，点击可刷新凭证）；未保存 App ID/Secret 时禁用并提示先保存。新增 IPC `messaging:enableFeishuMcp` + plugins.ts `upsertFeishuMcp`（覆盖式写入防旧凭证残留）+ preload `window.pi.messaging.enableFeishuMcp`，顺带给 `getMcpServers` 补上 `Promise<McpServerInfo[]>` 类型；手册 zh/en §18.3 同步。

   验证方式：① 消息接入 → 飞书（已保存凭证）→ 点「🔌 启用飞书 MCP」→ toast 成功、按钮变「✓ 已启用」。② 扩展功能 → 我的 MCP → 列表出现 lark-mcp（stdio）。③ 新会话里让智能体用飞书发消息/读文档 → 工具调用成功。④ 在 My MCP 停用后重开面板 → 按钮回到「启用」态。

11. **「消息接入」渠道会话工具只加载到聊天渠道占用的会话（省 token + 修 ID 不匹配）**：`mpi_channel_*` 三个桥接工具（列举/切换/新建渠道会话，供智能体处理正则未覆盖的自然语言指令）此前经 `--extension` 加载进**每个**会话——Windows 桌面与定时任务用不上却每次 LLM 请求多付 ~300 token，且可能干扰语义判断。现在 spawn 时按「该线程当前是否被飞书/微信占用」决定：只有渠道占用的会话才带这三个工具（ipc.ts `createHandle` gate + automation.ts 移除）；桌面会话、定时任务、热备进程完全不再携带。顺带修复一个 ID 不匹配 bug：扩展端从会话文件名取原始 UUID 作 sessionId，而主进程注册表此前用远程不透明 ID（HMAC）作键——两者永不相等，LLM 兜底命令在生产环境始终报 not_channel_session。现注册表统一按**会话文件 UUID** 为键（两端都能算出的唯一 id），服务侧新增 `resolveSessionUuid` 解析（ipc.ts 经 remoteThread 实现）。已知边界：bridge 已运行后才被渠道占用的线程（如桌面先打开的会话后被 /use 切过去、或新建渠道会话的首次生命周期）要等下次 spawn 才带工具——期间快速路径命令（/new /list /use + 短自然语言短语，主进程处理）不受影响。测试 `test:channelcmd` 补 UUID 提取/gate 判定用例。

   验证方式：① Windows 端开任意会话问「有哪些会话」→ 智能体不会调用渠道工具（无此工具），行为与之前桌面体验一致；长会话的 token 消耗不再含 ~300/请求的工具定义。② 飞书发正则未覆盖的自然语言指令（如较长句式的「帮我切到之前聊天气的那个对话」）→ 智能体经 mpi_channel_* 工具完成切换并回复确认（此前会报 not_channel_session）。③ 定时任务运行期间不加载渠道扩展（主进程日志/行为无变化，仅省 token）。

12. **模型「上下文长度」支持自动探测（免手填）**：设置 → 模型与提供商的每个模型行，上下文长度输入旁新增「自动」勾选。勾上且留空时，保存由 MPI 按三级链解析：① 查 pi runtime 内置模型目录（pi-ai 自带约 1200 个模型的 contextWindow 全量目录，精确 + `/` 后缀匹配——目录里 `qwen/qwen3-max` 命中裸 id `qwen3-max`）；② 未命中则探测提供商 API（OpenRouter 系网关 `/models` 的 `context_length`；LM Studio 的 OpenAI 兼容端点无 context 字段，改探其原生 REST `/api/v1/models`；Ollama best-effort 解析 `/api/show` 的 num_ctx——仅本地地址会探测，公网 API 最多一次请求）；③ 仍未找到则留空回退 Pi 默认 128K。解析值写回 models.json（Pi 需要具体数字），带 `contextWindowAuto`/`contextWindowSource` 标记；输入旁显示来源徽标（内置目录 / API 探测 / 未探测到·默认128K）。**字段已有值则保存时不重新解析**（手填或此前解析过均尊重原值）；点输入右侧「↻」可显式重探并覆盖当前值（本地模型改 num_ctx 后用）。新增 `src/main/model-context.ts` + IPC `settings:resolveModelContext`；测试 `test:model-context`。

   验证方式：① 设置 → 模型与提供商 → 展开任一供应商编辑器 → 给某云端模型（如 qwen3-max）勾「自动」并留空上下文长度 → 「保存模型配置」→ 输入框自动填入解析值，旁现「内置目录」徽标。② 本地 LM Studio 供应商：勾自动 + 留空 → 保存 → 显示当前加载模型的上下文（原生接口探测），徽标「API 探测」；在 LM Studio 里改 num_ctx 后点该行「↻ 重新探测」→ 值更新。③ 某模型手填一个值 → 多次保存 → 值永不被改动、不触发探测。④ 目录与 API 都查不到的模型 id → 保存后输入旁显示「未探测到 · 默认128K」，上下文圆环 gauge 以 128K 为分母。

13. **模型「Auto」模式（自动切换模型）**：会话的模型下拉顶部新增「Auto（自动切换）」项——开启后 MPI 按设置里的候选池自动选模型：**免费优先 → 质量档高→低 → 延迟低→高**。健康信号完全被动采集自既有 agent 事件（首 token 时间 TTFT + 错误分类，每模型滚动最近 5 轮），零额外 API 调用；只有需要切换决策时才主动探测候选（最小 HTTP 补全 max_tokens≈1、20s 超时、同模型每小时最多 3 次、并发探测）。触发规则：硬失败（认证/限流/服务端错误）或连续软降级（TTFT > 自身中位数×3 且绝对值 > 15s，默认连续 2 轮）→ 切到最佳可用候选；**只切到不低于当前质量的档位**，全部低于时「救急降级」（带警告 toast，可设严格模式禁止）；处于备用状态时每 5 分钟重探更优候选，恢复且过 10 分钟冷却后自动切回（防抖动）。质量档按模型自动推断（前沿模型表 → 参数量信号如 `35b-a3b`/`27b` → pi-ai 目录价格带），池编辑器可手动覆盖；「收费」标记默认免费、由用户勾选——有健康免费候选达标时绝不选收费，只有走不通才用收费（toast 注明）。**非 auto 会话零开销**：不监控、不探测，退出 auto 立即停止该线程全部信号采集。新空会话开 auto → 显式选定最佳健康候选为初始模型（顺带修复「新会话落在 pi 全局默认模型」的漂移问题）；切换只发生在轮次之间、绝不打断流式输出；手动选具体模型 = 退出该会话 auto。设置 → 模型与提供商底部新增「自动模型」卡：候选池编辑器（增删/上下移 + 收费勾选 + 档位覆盖）+ 策略参数（慢速倍数/阈值秒数/连续次数/恢复探测间隔/切换冷却/允许救急降级/切换通知）。pill 上绿点=auto 正常、橙点=警告态（用了收费或已降级），切换后上下文圆环自动按新模型刷新。新增 `src/main/model-autopilot.ts`（纯逻辑 + 注入回调，无 electron 依赖）、IPC `thread:setAutoModel`、事件 `pi:autoModel`；config.json 新增 `autoModels`/`autoModelThreads`；测试 `test:model-autopilot`。

   验证方式：① 设置 → 模型与提供商 → 底部「自动模型」卡 → 「＋ 添加候选模型」加两个以上（如本地 qwen3.8-27b + deepseek-v4-flash，后者勾「收费」）→「保存自动模型配置」。② 会话模型下拉点「Auto（自动切换）」→ toast「auto 模式已选用：xxx」（最佳健康免费候选），pill 出现绿点；再手动选某个具体模型 → auto 退出、绿点消失。③ 让当前模型不可用（如关掉本地 LM Studio）后发一条消息 → 报错后自动切到池内其他候选 + toast「已自动切换：A → B（原模型不可用）」，pill 显示新模型名。④ 备用期间恢复原模型 → 约 5 分钟内自动切回 + toast「（更优模型恢复可用）」。⑤ 无免费候选达标时 → 用收费模型且 toast 注明「（无免费候选，使用收费模型）」、pill 橙点。⑥ 非 auto 会话：不产生任何探测请求；退出 auto 后信号采集立即停止。

14. **应用图标更换为「WW 雨伞」新设计**：窗口/任务栏/安装程序图标全部换成新的黑色雨伞 WW 线条标志（白色圆角底、四角透明，屏幕尺寸与旧图标一致）。源图存于 `resources/umbrella.jpg`；`scripts/generate-icon.py` 重写为新流程（裁边 + 去 JPEG 噪点 + 合成到与原图标同几何的白色 squircle），重新生成 `icon.png` / `icon.ico`（16–256）/ `icon.icns`。

   验证方式：① 完全重启 MPI → 标题栏左上角、任务栏、设置「关于」页均显示新雨伞图标。② Windows 资源管理器里已安装 exe 的图标可能因系统缓存仍显示旧图，重新安装后生效（属正常现象）。

15. **会话窗口新增会话内消息搜索（Ctrl+F）**：会话顶部工具栏置顶星形图标左侧新增 🔍 按钮——点击或按 Ctrl+F 打开会话内搜索条：在当前会话的用户消息与智能体回复正文中做大小写不敏感的子串匹配（不含思考块与工具调用参数）；右侧显示「当前/总数」匹配计数，无结果显示「无匹配」；有匹配时非命中消息变暗、当前命中的消息带高亮描边并闪烁提示；Enter / ↓ 下一个、Shift+Enter / ↑ 上一个（首尾循环），自动滚动到匹配处居中；Esc 或 × 关闭。只搜索已定稿的消息（流式尾巴不含）；切换会话时重置。新增纯函数 `lib/chat-search.ts` + 测试 `test:chatsearch`；手册 zh/en §14 与快捷键表同步。

   验证方式：① 打开任一有内容的会话 → 按 Ctrl+F（或点星形图标左侧的 🔍）→ 顶栏下方出现搜索条且输入框自动聚焦；输入一个对话里存在的关键词 → 计数显示「1/N」、非命中消息变暗、首个命中带描边闪烁并滚动到居中。② 连按 Enter → 逐个跳到后续匹配（末尾循环回第一个）；Shift+Enter 反向跳。③ 输入不存在的词 → 显示「无匹配」。④ Esc 或 × 关闭搜索条；切换到别的会话再回来，搜索状态已重置。

16. **使用手册整体重写（自然语言）+ 目录锚点应用内可跳转**：内置使用手册（zh/en 双份）全文按自然语言梳理——去掉生硬的排比句式与实现术语、长句拆短、加粗只保留需要找的界面元素；顺带修正三处与代码不符的描述（帮助菜单含「使用手册」项、设置页 tab 实际顺序、「查看更新日志」入口在「关于 MPI」tab）。同时 Markdown 渲染管线加入 rehype-slug：标题生成 GitHub 风格 id，文档内锚点链接（如手册目录）点击后平滑滚动到对应标题；`#anchor` 链接不再走 target=_blank。新增测试 `test:slugs`（校验两份手册全部目录锚点与真实 slugger 输出一致）。

   验证方式：① 帮助 →「使用手册」→ 打开手册，点目录里任意章节（如「12. 扩展功能」或「4. 配置模型」）→ 平滑滚动到对应标题。② 英文界面下同样可跳转。③ 聊天消息里的 Markdown 外链行为不变（仍在系统浏览器打开），普通文件预览、代码高亮不受影响。

17. **应用自更新启用并指向 MPI 自己的 GitHub 仓库**：帮助 → 关于 MPI 的「MPI 应用更新」卡现在真正可用——从 github.com/wjw1245038311/MPI 的 Releases 检查更新（此前该功能被禁用）。发现新版本后可在应用内下载 Windows 安装包并安装重启；GitHub 上还没有 release 或网络不通时显示中文提示（「还没有发布版本」/「无法连接 GitHub」）而非原始英文报错。发布流程：大版本打包后把 `dist/` 里的 `MPI-Setup-x.y.z.exe` + `latest.yml`（+ `.blockmap`）作为附件挂到 GitHub 对应 Release；日常开发不受影响——开机不自动检查，只有手动点「检查最新版本」才访问网络。

   验证方式：① 重新打包安装后，帮助 → 关于 MPI →「MPI 应用更新」卡 → 点「检查最新版本」→ GitHub 还没有 release，显示「检查失败：GitHub 仓库还没有发布版本（或无法访问该仓库）」（不是原始英文报错）。② 在 GitHub 建一个 Release（如 v0.5.x）并附上 dist/ 产物 → 再检查 → 显示最新版本号 +「可更新」徽标；点「下载 vX.Y.Z」→ 进度条走完出现「安装并重启」。③ dev 模式（npm run dev）下该卡不可安装（行为不变，不报错）。

18. **「消息接入」新增微信渠道（个人微信 iLink bot，扫码连接）**：面板「通道」列的微信行正式可用——点「📱 扫码连接微信」→ 手机微信扫码确认 → bot_token 直接写入本机 config.json（与飞书同策略，凭证永不跨 IPC），无需注册企业、配置回调地址；部分账号需按提示输入数字验证码。走 iLink `getUpdates` 长轮询（无需公网 IP）：私聊发文本即驱动绑定项目下名为「微信接入」的专属会话，处理中显示「正在输入」，完成后**单独发一条结果消息**（iLink API 无原地更新原语，与飞书流式刷新不同）。命令 `/new`、`/list`、`/use <序号>`、`/help` 与飞书共用同一套 ChannelBase 实现；会话权限沙盒/完全同飞书。仅支持私聊与文本消息（图片/文件暂不支持）；一个微信账号绑定一个 MPI 实例；get_updates_buf 游标持久化防重启重复投递，token 失效自动停渠道并提示重新扫码。这是腾讯灰度中的个人微信 bot 通道——账号不在开放范围内时扫码无反应或报不可用。新增 `src/main/messaging/wechat-service.ts` / `wechat-registration.ts` / `weixin/ilink.ts`、IPC `wechat:getState/setConfig/startQrLogin/cancelQrLogin/submitVerifyCode` + preload `window.pi.wechat`、config.json `wechatChannel`；测试 `test:wechat`。

   验证方式：① 导航栏 →「消息接入」→ 左侧点「微信」行（不再置灰）→ 右侧出现状态灯 + 「扫码连接微信」卡 → 点按钮出二维码 → 手机微信扫码确认（部分账号需输入验证码）→ 显示「已绑定微信 · <打码 botId>」。② 选绑定项目、开总开关、「保存配置」→ 状态灯变绿。③ 微信私聊该 bot 发文本 → 出现「正在输入」，随后收到一条结果消息；MPI 绑定项目下出现/复用名为「微信接入」的会话且内容一致。④ 发 `/new`、`/list`、`/use 2`、`/help` → 与飞书渠道行为一致。⑤ 重启 MPI → 不重复处理旧消息（游标持久化生效）。

## v0.5.3（2026-09-09）

1. **导航栏文件可拖拽到输入框**：导航栏「文件」tab 的文件行可直接拖到 Composer 输入区，松手即作为附件加入（与「添加文件」按钮同一条链路；目录不可拖）。拖悬在输入区时输入框显示高亮描边提示可放置；同一文件重复拖入不会产生重复附件。

   验证方式：① 打开任一项目 → 导航栏切到「文件」tab → 按住任意**文件行**拖到底部 Composer 输入框——拖悬时输入框出现绿色高亮描边，松手后该文件出现在输入框上方附件列表（显示文件名），可随消息发送。② 同一文件再拖一次不产生重复附件。③ 目录行不可拖动；从资源管理器把文件拖进输入框的原有行为不变。

2. **预览栏新增「在资源管理器中显示」按钮**：预览任意文件时，标题栏（刷新按钮左侧）出现文件夹图标按钮，点击即在系统文件管理器中定位并选中该文件（`shell.showItemInFolder`），方便快速找到文件实际位置。新增 IPC `app:revealFileInExplorer`；文件不存在时报错 toast。

   验证方式：① 导航栏「文件」tab 点开任一文件 → 预览标题栏出现文件夹图标按钮（刷新左侧）→ 点击 → 系统资源管理器打开该文件所在目录并选中它。② 英文语言下 tooltip 显示 “Show in File Explorer”。

3. **重启后自动回到上次关闭时的会话**：退出 MPI 前停留在哪个会话，重新打开应用就自动进入该会话（历史即时渲染、pi 进程后台连接）。若该项目/会话已被归档或删除则静默跳过；若退出前已手动关闭全部会话，则不恢复任何会话。

   验证方式：① 打开任一项目的一个会话 → 完全退出 MPI → 重新打开 → 直接落在该会话（历史完整、可继续发送）。② 关闭所有会话后再退出重开 → 停在空状态。③ 把上次的会话归档/删除后重开 → 不自动进入任何会话，也不报错。

4. **预览栏支持浏览器式 tab 页 + 拖拽 + 独立窗口**：
   - **tab 页**：可同时打开多个文件预览，顶部 tab 条（图标+文件名+×），点击切换、× 关闭（关活动 tab 时自动切到相邻）；再次点开已打开的文件会刷新该 tab。面板内不再重复显示文件名（只保留在 tab 条上）。导航栏文件行高亮改为「有任意打开的 tab」即高亮。
   - **拖拽**：tab 之间可拖动排序（绿色插入指示线，同置顶区交互）；导航栏「文件」行的文件可直接拖到预览面板任意位置开新 tab（从资源管理器拖入同样支持），拖悬时面板显示高亮描边。tab 支持 Visual Studio 式分离/停靠：**把 tab 拖出应用窗口外松手 → 移动到独立浮动窗口（出现在松手处而非屏幕中央；主面板该 tab 消失，VS 文档窗口语义）**；放到另一个 tab 上是排序；在应用内其他位置松手则弹回原位。
   - **独立窗口 + 停靠**：两种打开方式——右键任一 tab →「在独立窗口打开」= **复制**（原 tab 保留，两边可同时看同一文件）；拖出 = **移动**。独立窗口为无边框 + **单层自绘标题栏**（VS 式浮动文档窗口，图标+文件名+刷新/关闭）：**按住整条顶栏拖动 = 移动窗口，拖到主窗口预览面板的 tab 条区域上松手 → 停靠回来**（松手在其他位置只是移动窗口）。同一文件重复弹出会聚焦已有窗口而不是再开一个。拖动过程中窗口尺寸锁定，不会因跨显示器 DPI 变化而缩放。

   验证方式：① 导航栏「文件」tab 依次点开两个不同文件 → 预览顶部出现两个 tab，点击可来回切换、内容不丢失；点 × 关闭其中一个 → 自动切到剩下的。② 按住一个 tab 拖到另一个前面/后面松手 → 顺序改变（拖动时有绿色指示线）；把 tab 拖出应用窗口外（如桌面）松手 → 该 tab **移动**到独立浮动窗口（主面板里消失，VS 式分离）；在应用内其他位置松手 → 弹回原位不弹窗。③ 把导航栏里的文件行拖到预览面板上松手 → 新开一个 tab 并显示该文件；拖悬时面板有绿色描边。④ 右键任一 tab → 「在独立窗口打开」= 复制：弹出新窗口（单层自绘标题栏，按住可拖动）显示同一文件，主面板原 tab 保留、两边可同时看；再对同一文件点一次「在独立窗口打开」→ 只聚焦已有窗口不新开。⑤ 停靠：按住独立窗口的整条标题栏（文件名那一行）拖到主窗口预览面板的 **tab 条上**松手 → 该窗口自动关闭、主面板对应 tab 被激活；松手在聊天区等其他位置 → 不停靠，浮动窗口停在原地；拖动过程中窗口大小保持稳定。

5. **上下文弹窗压缩按钮移位**：从弹窗头部（刷新旁边）移到「已压缩 N 次 / Compactions N×」数字后面，与它作用的计数挨在一起更直观；头部只留刷新按钮。禁用条件不变（未连接/流式中/压缩中/无消息时置灰）。

   验证方式：点输入框左侧圆环按钮打开上下文弹窗 → 压缩图标现在在「已压缩 N×」数字

6. **设置里「应用更新」与「关于」合并为「关于 MPI」**：原「关于」tab 只有一行软件版本，与应用更新卡片的「当前版本」重复，已移除；「应用更新」tab 改名为「关于 MPI」，内含应用更新 + Pi 核心更新两张卡片。

   验证方式：打开设置 → 左侧不再有单独的「关于」tab，最后一项为「关于 MPI」；点进去可见应用更新卡片（当前/最新版本、检查/下载/安装按钮）与 Pi 核心更新卡片。

7. **帮助菜单新增「使用手册」与「关于 MPI」面板**：帮助 → 使用手册在预览 tab 中打开内置用户手册，**按界面语言自动选择版本**（中文 user-manual.md / 英文 user-manual-en.md，均随安装包分发）；帮助 → 关于 MPI 弹出面板，内容与设置里「关于 MPI」tab 相同（应用更新 + Pi 核心更新两张卡片）。两张更新卡片抽成共享组件 AboutPanels.tsx，设置与弹窗复用。

   验证方式：① 顶部菜单 帮助 → 使用手册 → 预览面板新开 tab 显示手册 Markdown，切换界面语言后重开分别得到中/英文版（菜单项本身也随语言显示 User manual）；② 帮助 → 关于 MPI → 弹出「关于 MPI」面板，含应用更新（检查/下载/安装）与 Pi 核心更新两张卡片，点遮罩或 × 关闭；③ 设置里「关于 MPI」tab 功能不变。右侧，点击仍可正常触发压缩（等同 /compact），头部只剩刷新按钮。

## v0.5.2（2026-09-09）

1. **「分支 / 克隆」对齐 pi 官方语义**：此前两个按钮行为完全相同（都走自定义扩展命令 `/mpi-branch-at`，从所点回复处分叉）。现按 pi 文档（pi.dev/docs/latest/usage）区分：
   - **分支（Fork）**：移到每条**用户消息**的悬停操作条上（原智能体回复下的两个按钮移除）。走 pi RPC 原生 `fork` 命令——从该提问之前分叉出新会话，并把被分叉提问的原文自动填入输入框（可修改后发送），与 TUI `/fork` 行为一致。
   - **克隆（Clone）**：改为**整段会话复制**（pi RPC 原生 `clone`，当前活跃分支完整复制到新会话文件）。入口从消息下方移到侧栏会话行右键菜单「克隆会话」。
   - 内部清理：移除 `/mpi-branch-at` 扩展命令与 `bridge.branchAt`；fork/clone IPC handler 改用原生命令并透传 cancelled。

   验证方式：① 悬停任意一条**用户消息**——气泡下方出现 [复制][分支] 两个按钮（智能体回复下不再有分支/克隆按钮）；点「分支」→ toast「已从所选消息创建分支，原提示词已放入输入框。」，新会话切为当前、原会话标记断开，且输入框自动带上那条提问的原文（可编辑后发送）。② 侧栏右键任一会话 → 菜单出现「克隆会话」（置顶与删除之间）；点击 → toast「已克隆会话（完整复制当前分支）」，新会话包含全部历史并切为当前。③ 流式回复进行中点分支/克隆会提示等待结束。

2. **全仓 review 清理**：移除死代码与未使用依赖（净删 ~194 行）——未使用依赖 clsx/skills；死 IPC ×3（app:unpinProject / thread:getModels / thread:getCommands）；window:maximized-changed 三层死链；fs-service/plugins/runtime-package/remote-service 多处死函数与未用导入；App.tsx sidebarOpen 无效订阅（顺带消除每次折叠导航栏的全树重渲染）。修复 ChangelogModal 的 `CHANGELOG.md`→`changelog.md` 导入大小写（Windows 掩盖的跨平台构建隐患）。

   验证方式：应用功能无变化；「关于/更新日志」弹窗正常显示 changelog 内容即可确认第 2 条的导入修复生效。

3. **扩展包安装不再依赖系统 Node.js/npm**（修复未装 npm 的电脑上无法安装扩展包的问题）：
   - 独立 runtime 现内置 npm CLI（`MPI-Runtime-*.tar.gz` 新增 `npm/node_modules/npm`，压缩后约 +2.5MB）。每次执行 pi install/remove/update 前，MPI 自动把 `~/.pi/agent/settings.json` 的 `npmCommand` 指向内置 node + npm-cli.js——用户已显式配置过（如 mise/asdf）则绝不覆盖；runtime 更新后旧的管理值自动刷新。
   - Pi 走「registry 直下」路径更新时，新 runtime 从旧 runtime 继承内置 npm；存量安装（旧 tarball 解压的 root 无 npm/）首次执行包命令时自动从安装包内嵌 archive 只提取 `./npm` 子树补齐。
   - **友好报错**：安装/移除/更新因缺 npm 或 git 失败时，不再显示难懂的 `spawn npm ENOENT`——npm 缺失提示先装 Node.js LTS（nodejs.org），git 缺失提示先装 Git（git-scm.com）；移除场景额外说明「已从列表移除但本地文件未清理」。
   - 新增纯逻辑模块 `src/main/npm-command.ts`（决策矩阵：首写/已正确不动/旧管理值刷新/尊重用户配置/清理失效值 + ENOENT 分类），测试 `npm run test:npmcommand`。

   验证方式：① 在**未安装 Node.js** 的电脑上打开「扩展功能 → 扩展包」，从市场装任一包（如 pi-mcp-market）——安装成功；随后查看 `%USERPROFILE%\.pi\agent\settings.json` 出现 `npmCommand`，指向 `%APPDATA%\MPI\runtime\versions\…` 下的 node.exe 与 npm-cli.js。② 已装 Node.js 的电脑行为不变（内置 npm 优先）；若手动把 settings.json 的 `npmCommand` 改成自己的命令，MPI 不会覆盖它。③ git 源缺 Git 时：安装 `git:` 来源扩展包 → toast「此电脑未安装 git，无法从 git 源安装扩展包…」（可在 PATH 中临时移除 Git 目录模拟）。

## v0.5.1（2026-09-09）

1. **「扩展功能」面板改造**：顶层拆为 技能 / 扩展包 两个模块；技能模块下三个子 tab——我的技能 / Skill 市场 / 统计。
   - **我的技能**（主从布局）：左列搜索 + 全部/已启用/已禁用过滤 chips + 按来源目录分组列表（Pi 目录 / .agents 目录，状态点+描述摘要+开关）；右列详情栏——徽章、「复制命令」（/skill:name）、「启用/停用」按钮、命令/路径 meta 行、说明区、SKILL.MD 渲染预览 +「复制 Markdown」。新增 IPC `plugins:getSkillContent`（main 侧校验路径在受管技能根下才读文件）。
   - **Skill 市场**：原 skills.sh 搜索/安装整体挪入；「其他市场」外链卡片（虾评 / SkillHub / SkillsMP）放在搜索框上方的固定横条，常驻可见、无需滚动即可点到。
   - **统计（一期）**：总数/已启用/已禁用统计卡 + 按来源目录分布条 + 已禁用技能列表（可一键重新启用）。调用量排行（7/30/90 天）为二期，本次未做。
   - **扩展包模块**：拆「我的扩展包 / 扩展包市场」两个子 tab。我的扩展包参考「我的技能」主从布局——顶行紧凑按钮组（＋安装扩展包 / 更新全部），安装表单默认折叠、点开后为内联输入框（限宽 640px，Enter 安装/Esc 收起）+ 跳转市场子 tab 的提示链接；左列搜索 + 全部/已启用/已禁用 chips + 按类型分组列表（npm/git/local，状态点+来源摘要+开关）；右列详情栏——徽章（类型/版本/启用态）、「更新」「启用/停用」「移除」按钮、来源与安装目录 meta 行、package.json 说明。新增 IPC `plugins:getPackageInfo`（只读 settings.json 已登记包的已装 manifest：npm/git 查 ~/.pi/agent/npm/node_modules，local 读本目录）。顺带修复 scoped npm 包名显示丢 @scope 前缀的问题。
   - **扩展包市场**：搜索公共 npm registry（`registry.npmjs.org/-/v1/search`，免鉴权），默认查询 “pi extension” 直接呈现 pi 相关扩展；左列搜索 + 结果列表（包名/版本/描述摘要/安装按钮，已安装的显示「已安装」标记）；右列详情栏——徽章（版本/license/周下载量）、npm 页面与仓库链接、发布日期、说明、关键词、README 渲染预览。一键安装走现有 `npm:<name>` 安装链路。新增 IPC `plugins:npmSearch` / `plugins:npmReadme`。
   - 面板加宽至 1080px。

   验证方式：① 打开「扩展功能」——顶部 [技能 N][扩展包 M] 两个模块 tab + 右侧刷新按钮；默认技能模块，下方三个子 tab。② 我的技能：点左列任一技能 → 右栏显示标题/徽章/命令路径/说明/SKILL.MD 预览；「复制命令」把 /skill:name 写入剪贴板（toast「命令已复制」）；开关启用/停用与详情徽章同步。③ Skill 市场：「其他市场」横条（三张卡片）在列表上方常驻可见、可直接点击；skills.sh 搜索/安装照常。④ 统计：卡片数字与列表一致、分布条百分比合计 100%、禁用技能行可一键重新启用。⑤ 扩展包模块：[我的扩展包][扩展包市场] 两个子 tab；我的扩展包顶行只有两个紧凑按钮（＋安装扩展包 / 更新全部），点 ＋ 展开内联安装输入框（Enter 安装、Esc 或再点 × 收起，下方提示可直接跳到市场子 tab）；左列按 npm/git/local 分组列表 + 右列详情栏；点任一包显示类型/版本徽章、来源与安装目录、package.json 说明；详情栏内更新/启用停用/移除可用；scoped 包（如 @juicesharp/rpiv-todo）显示完整名。⑥ 扩展包市场：默认展示 “pi extension” 搜索结果（前 30 条，按相关度排序）；点任一包显示版本/license/周下载徽章、npm 页面与仓库链接、说明/关键词、README 预览；列表或详情栏「安装」以 npm:<包名> 安装，成功后该行变「已安装」标记且出现在我的扩展包；输入关键词可缩小搜索范围。

2. **「插件」面板改名「扩展功能」**：英文与 VSCode 的扩展翻译一致，统一为 "Extensions"。侧栏入口按钮、面板标题/返回按钮/搜索占位符/刷新提示、斜杠命令菜单的分类标签与搜索框、空状态页描述、设置 Pi 更新区提示（「扩展请在『扩展功能』面板更新」）等全部中文文案同步；英文统一 Extensions（单数标签 Extension）。内部标识符（IPC 通道 plugins:*、CSS class、文件名）不变。

   验证方式：① 侧栏按钮显示「扩展功能」，点开后面板标题为「扩展功能」（副标题「管理 pi 的扩展包与技能」）；进技能中心后返回按钮为「← 扩展功能」。② 语言切英文——侧栏 Extensions、面板标题 Extensions。③ 输入框敲 / ——斜杠菜单头为「命令、扩展功能和技能」，扩展来源条目的分类标签为「扩展功能」（英文 Commands, extensions & skills / Extension）。

3. **「扩展功能」新增「MCP」模块**：顶层 tab 变为 [技能 N][扩展包 M][MCP K]；MCP 模块下两个子 tab——我的 MCP / MCP 市场。
   - **gate 在 pi-mcp-market 扩展**：未安装时显示不可用卡片 + 一键 `npm:pi-mcp-market` 安装按钮（说明实际连接与运行服务器还需 pi-mcp-adapter）；当前实例 preload 缺 MCP 接口时提示完全重启。
   - **我的 MCP**（skill 式主从布局）：顶部依赖扩展条显示 pi-mcp-market / pi-mcp-adapter 启用状态（adapter 缺失可一键补装、已装可开关）；左列搜索 + stdio/remote 过滤 chips + 服务器列表行（名称/类型徽章/描述摘要/停用开关）；右列详情栏——命令复制、「启用/停用」（只写 mcp.json 的 disabled 标志，可随时恢复）、「删除」（带确认）。配置读写 `~/.pi/agent/mcp.json`（与 pi-mcp-adapter / pi-mcp-market 共享），保留 imports 等其他键；底部附 /mcp-market 使用指引卡。
   - **MCP 市场**：浏览 mcpmarket.cn 公开目录（`/api/clients` 搜索 + 分页「加载更多」），卡片显示 logo/作者/stars/简介，详情栏 zh/en overview 分节 + GitHub 链接 + 站点外链；市场卡片一键安装暂缓（待办 P1-9——该站无公开配置 API）。
   - 新增 IPC `plugins:getMcpServers/setMcpServerDisabled/removeMcpServer/mcpMarketSearch/mcpMarketDetail` + 主进程 mcp-market.ts。

   验证方式：① 打开「扩展功能」——顶层 tab 为 [技能][扩展包][MCP]（各带计数徽标）；MCP 模块下两个子 tab。② 未装 pi-mcp-market 时显示不可用卡片，点安装成功后模块可用。③ 我的 MCP：依赖条显示 market/adapter 状态、缺 adapter 可一键补装；左列列出 mcp.json 已配置服务器（stdio/remote 徽章），搜索/chips 过滤生效；点任一服务器右栏出现命令复制、启用/停用开关、删除按钮；停用后 mcp.json 该条目带 disabled:true，再开即恢复。④ MCP 市场：默认加载目录列表（logo/作者/stars/简介）；关键词搜索与「加载更多」可用；点卡片详情栏显示 zh/en overview 分节与 GitHub 链接。

4. **标题栏简化**：移除窗口标题栏左侧的折叠导航栏按钮与右侧的切换预览按钮——两处均已有等价入口（视图菜单、侧栏顶部折叠钮、聊天头「切换预览」钮），功能不丢失；i18n 与相关样式同步清理。

   验证方式：① 标题栏两侧不再有按钮，只剩品牌区/窗口控制。② 「视图」菜单里仍有「折叠导航栏 / 展开导航栏」「切换预览面板」；侧栏顶部折叠钮、聊天头「切换预览」钮照常可用。

5. **导航栏左下角优化**：移除与顶部标题栏重复的「设置」「帮助」按钮；手机远程控制小图标暂缓（记入待办 P1-10）。token 用量从点按钮弹浮层改为**常驻直显**——一行「今日用量 · 总用量」（令牌数，k/M 紧凑格式），悬停 tooltip 显示今日/合计明细、花费与总会话数；打开导航栏时自动加载，流式结束后即时刷新，另每 60 秒轮询一次（终端 pi 的活动也能被计入）。

   验证方式：① 导航栏左下角只剩一行「今日用量 · 总用量」数字——没有设置/帮助/手机图标。② 数字与实际消耗一致（今日=本地今天、总计=全部会话），悬停可见花费与会话数 tooltip。③ 发一条消息跑完后数字自动更新，无需点击；切英文显示 Today / Total。

6. **上下文用量移入输入框 + 圆环进度按钮**：原对话框顶部标题栏的「上下文」Gauge 按钮移到输入框底栏、模型选择器前面，按钮改为**圆环进度条**（弧线=占用比例，颜色沿用四档：≤60% 绿 / 60–74% 黄 / 75–89% 橙 / ≥90% 红）。点开浮层参考「上下文窗口」样式重排——标题 + 刷新、当前模型名、进度条、「~已用 / 总量 + 百分比」、提示行（压缩后估算值说明 + 「按当前已渲染消息计算，非累计账单 tokens」）、「已压缩 N 次」一行（悬停显示最近一次时间）、分档着色的压缩建议（文案不变）。「压缩上下文」从模型面板的「会话工具」移到浮层右上角、刷新按钮右侧的小图标（等同 /compact，悬停有说明，压缩中图标旋转）。圆环常驻自动刷新：切换会话时加载、流式结束/压缩完成即时刷新、运行中每 15s 轮询一次。

   验证方式：① 对话框顶部标题栏不再有 Gauge 按钮；输入框底栏模型选择器左侧出现一个圆环按钮。② 点开浮层——标题「上下文窗口」+ 当前模型名 + 进度条 + 「~X / Y + %」，颜色与压缩建议同之前一致；右上角刷新右侧多一个压缩小图标（模型面板的会话工具里已移除）。③ 发一条长消息观察：运行中圆环弧线逐渐增长（约每 15s），跑完后自动更新到最终值；占用超 60%/75%/90% 时弧线与数字依次变黄/橙/红。④ 点右上角压缩小图标——压缩中图标旋转，完成后浮层内数字自动刷新为压缩后的估算值（带 ~ 前缀）。

7. **模型面板思考等级改为圆润下拉**：模型选择弹层里的「思考等级」原来是一排 chip（等级多时换行占高），改为一行「标签 + 圆角胶囊按钮」——点击在下方内联展开圆角选项列表（选项含映射提示如 `中 → mid`，当前等级带对勾）；选中即生效并关闭弹层。输入框底栏的命令入口从「@ 命令」改为「/ 命令」（与斜杠命令一致）；导航栏「扩展功能」入口的 @ 图标换成插头（Plug）图标，更贴合插件语义。

   验证方式：打开模型选择弹层——「思考等级」是一行标签+胶囊按钮，点击展开圆角选项列表（不再是一排 chip、也不是系统原生下拉）；选一个等级后弹层关闭、输入栏模型 pill 上的思考等级标记同步更新。输入框左下角命令按钮显示「/ 命令」；导航栏「扩展功能」前是插头图标。深色主题下胶囊与选项背景正常。

8. **修复设置页下拉箭头在深色主题消失**：`.set-select` 的 dark 规则用了 `background:` 简写，把自定义箭头的 `background-image` 冲掉了；改为长写法（新增控件时同样避开此坑）。

   验证方式：切深色主题打开设置里任意下拉框——右侧小箭头正常显示。

9. **消息列去掉宽度上限**：`.messages`（及扩展提示卡 `.extui-card`）残留 `max-width: 860px` + 居中，窗口横向拉宽到一定程度后输入框跟着变宽、消息却不再变宽，看起来像「对话框无法再横向拉伸」。现移除上限——消息列与卡片随窗口宽度一起拉伸（与 v0.4.15 去掉 .composer max-width 的行为一致）。

   验证方式：把窗口横向拉宽——消息区左右留白持续收窄、消息与输入框同步变宽，不再固定 860px 居中一列；缩窄时布局正常不溢出。

10. **Pi TUI 终端模式**：对话框顶栏新增「终端」切换按钮（在「切换预览」左侧）——点击后整个对话框变成交互式 pi 终端（xterm.js + node-pty/ConPTY，与 GUI 模式共用同一会话文件与工作目录，历史双向共享）。进入 TUI 前若正在回复会先自动中止；退出时（点按钮、pi 自己 /exit、或切走会话）终端进程结束，RPC bridge 从磁盘重新加载会话。顶栏按钮全部集中到右侧（原来置顶/重命名浮在中间）。新增依赖：@xterm/xterm、@xterm/addon-fit、node-pty（N-API 预构建，打包配置 npmRebuild:false + asarUnpack）。

   竞态安全：主进程 tui:start/tui:stop 用「代际令牌」处理乱序 IPC——dev 下 React StrictMode 会双挂载 effect（start→stop→start），快速连点切换也会让 stop 落在 start 完成之前；旧实现会让迟到的清理 stop 误杀新终端、或泄漏孤儿 pi 进程，并触发假 tui:exit 把界面弹回 GUI。现在：被取代的 spawn 自杀、条件式 stop（带 gen）只杀自己启动的那个 PTY、只有 pi 主动退出才发 tui:exit。自动化测试 npm run test:tuirace。

   验证方式：① 对话框顶栏按钮全部靠右，中间只剩标题+文件夹路径。② 点终端图标——会话区变成 pi TUI 终端并带出历史，可直接在终端里对话；③ 在终端发几条消息后点按钮切回 GUI——新消息出现在会话里（bridge 已重载）；④ TUI 模式下切换到别的会话自动结束终端，再切回来看到最新历史；⑤ 窗口缩放时终端尺寸跟随、文字不溢出；⑥ 快速连续进出 TUI（1 秒内点多次）不出现「终端一闪就弹回 GUI」或残留 node 进程。

## v0.4.21（2026-09-08）

1. **归档回收 tab 优化（改名 + 分组可视化 + 搜索）**：设置页标签「归档与回收站」改名为「归档回收」（相关提示/确认文案同步）。已归档会话与回收站条目改为**按项目分组**——组头显示项目名 + 计数徽标，点击折叠/展开（默认全展开），组内按时间倒序；行信息瘦身：归档会话行不再显示完整 JSONL 文件路径（移入悬停 tooltip），第二行只显示归档时间（今天=HH:mm、今年=「9月8日 14:30」、更早=完整日期）；回收站行第二行为删除时间 + 大小。tab 顶部新增**搜索框**，同时过滤已归档项目 / 已归档会话 / 回收站条目（标题、项目名、路径子串匹配，不区分大小写），有查询时右侧显示结果数并强制展开所有分组，各区块独立显示「没有匹配的…」空状态；关闭设置再打开自动清空搜索。归档会话现在记录归档时间（archivedAt）；旧数据无该字段则不显示时间并在组内排最后。

   验证方式：① 设置侧栏标签名为「归档回收」。② 从不同项目各归档一两个会话——「已归档会话」按项目分组，组头为项目名 + 计数；点组头可折叠/展开；行只显示标题 + 时间（无长文件路径），悬停行可见完整路径 tooltip。③ 删几个会话后看回收站区块——同样按项目分组，每行第二行为删除时间 + 大小。④ 顶部搜索框输入关键词——归档项目/会话/回收站条目同时过滤、右侧显示「N 条结果」；清空查询恢复原样。

2. **Ctrl+K 全局搜索支持「归档回收」+ 结果内一键恢复**：全局会话搜索弹窗（Ctrl+K）输入行右侧新增「归档回收」复选框——勾选后同时全文搜索已归档会话与回收站条目（与普通会话同样的标题/消息内容匹配），结果行带状态徽标（「已归档 / 项目已归档 / 回收站」，回收站为红色），每行附「恢复」按钮可直接恢复到侧栏（归档会话→取消归档；整个项目被归档→恢复项目；回收站→从回收站恢复），成功后该行自动从结果中移除。另修复搜索弹窗右上角「退出」键此前只是提示标签、点击无反应的问题——现在是真正的关闭按钮（Esc 仍可用）。

   验证方式：① Ctrl+K 勾选「归档回收」，输入只存在于某个已归档/回收站会话里的关键词——结果行出现对应徽标；点该行「恢复」，toast 提示后它回到导航栏、结果行消失。② 不勾选时搜索结果与之前一致（不含归档/回收站）。③ 点击弹窗右上角「退出」键直接关闭弹窗。

3. **UI 文案「侧栏 / 侧边栏」统一改名为「导航栏」**：切换/折叠/展开按钮 tooltip、恢复 toast（「会话已恢复到导航栏。」）、设置里归档与回收站的提示、拖拽调宽提示、预览收缩提示等所有中文文案同步更新；英文保留 "sidebar"。i18n exact 表键随中文原文同步改名（DOM bridge 按中文匹配）。

   验证方式：切换语言到 English，点标题栏折叠按钮——tooltip 仍显示 "Collapse sidebar"；切回中文显示「折叠导航栏」。恢复一个归档会话看 toast 文案。

4. **「自动化」改名「定时任务」**：侧栏入口、面板标题/提示、完成/失败 toast、无人值守报错等中文文案统一改为「定时任务」（更贴合按小时/天/周调度的实际行为）；英文保留 "Automations"。新建的定时任务会话名前缀为「定时任务: xxx」，旧会话名（「自动化: xxx」/「Automation: xxx」）仍会被识别并随语言切换本地化。

   验证方式：① 侧栏按钮与面板标题显示「定时任务」。② 跑一个定时任务看完成 toast「定时任务完成：xxx」。③ 旧版创建的「自动化: xxx」会话在侧栏正常显示，切英文变 "Automation: xxx"。

5. **修复 dev/升级安装后任务栏图标不更新**：Windows Explorer 按文件路径缓存图标——`resources/icon.ico` 原地换内容（dev 仓库内、安装包覆盖升级）时任务栏仍显示旧图。现在启动时把图标按内容 SHA-256 哈希复制到 `<userData>/icons/icon-<hash>.ico`，图标字节一变路径就变，Explorer 视为新文件不再命中旧缓存；同时清理该目录下历史版本残留。dev（%APPDATA%\MPI Dev）与正式安装（%APPDATA%\MPI）均生效。

   验证方式：完整重启 dev——任务栏按钮与托盘显示 WJW 竹蜻蜓图标；`%APPDATA%\MPI Dev\icons\` 下只有一个 `icon-<hash>.ico`。

## v0.4.20（2026-09-08）

1. **会话回收站（默认开启）**：删除会话不再直接永久删文件——默认先移入回收站（`<userData>/trash/`），只有从回收站里删除才算永久删除。设置 → 通用设置新增「回收站」开关行；关闭后删除恢复为立即永久生效（已入站的条目仍可恢复/清空）。设置页「归档与回收站」（原「已归档项目」标签）新增回收站卡片：显示条数、总占用，每行可「恢复会话 / 永久删除」（带确认弹窗），头部可「清空回收站」（带确认）。

   验证方式：① 侧栏删一个会话（垃圾桶图标或右键「删除」）——确认框文案为「将被移入回收站…」，删除后 toast 提示已移入回收站；设置 → 归档与回收站里能看到该条目（标题/项目/时间/大小），点「恢复会话」它回到原项目下。② 在回收站卡片对某条点「永久删除」——弹确认框，确认后条目消失且侧栏不再出现；头部「清空回收站」同样带确认并清掉全部。③ 通用设置取消勾选「回收站」后再删会话——确认框变为不可恢复警告，删除后不进回收站（之前入站的仍可在卡片里操作）。
2. **会话行常驻星形置顶切换 + 删除文案简化**：侧栏每条会话标题右侧新增始终可见的星形按钮——空心浅色=未置顶、点亮填充=已置顶，点击即置顶/取消置顶（与右键菜单、拖拽等效）。原「永久删除会话」文案统一改为「删除」（行图标悬停提示、右键菜单项、确认弹窗标题）。

   验证方式：① 侧栏任意会话行右侧始终能看到星形按钮；点击后星星点亮且该会话进入置顶区，再点取消。② 悬停删除图标提示为「删除」；右键菜单最后一项为红色「删除」；确认弹窗标题为「删除？」。

## v0.4.18（2026-09-08）

1. **界面用词统一为「会话」**：此前 UI 里「新建任务 / 线程」等叫法混用，现统一为「会话」（英文 Session，与常见 agent 客户端一致）。侧栏与空状态页按钮改为「新建会话」、标题栏菜单同步；侧栏标签页改名「会话」；新会话、暂无会话、置顶/取消置顶/恢复/永久删除会话、搜索弹窗（Ctrl+K）、上下文用量提示、设置里已归档会话等全部相关文案同步更新。归档按钮的悬停提示简化为「归档」。自动化面板的「定时任务」是另一概念保持「任务」不变；会话文件（session file）等技术词保留。

   验证方式：① 左侧栏顶部按钮显示「新建会话」、标签页为「会话/文件」；点项目上的 + 新建后标题为「新会话」，空项目下显示「暂无会话」。② 右键某条会话出现「置顶会话 / 永久删除会话？」；悬停归档图标提示「归档」，归档后 toast 提示到设置「已归档会话」中恢复。③ Ctrl+K 搜索弹窗占位符为「搜索所有会话中的关键词…」。④ 语言切英文：侧栏标签 Sessions、按钮 New session、右键菜单 Pin/Permanently delete session，不再出现 Thread/Task 混用。

2. **置顶区排序 + 对话框置顶按钮**：项目与会话的「置顶区」（列表顶部）现在支持手动排序——① 对话框上方（重命名旁）新增星形「置顶会话」按钮，点击置顶/取消当前会话，新置顶排在置顶区末尾；② 项目和会话的右键菜单新增「上移 / 下移」（仅置顶项显示，到边界自动隐藏）；③ 支持拖拽排序：置顶区内拖动换序（带落点指示线），把最近区的条目拖进置顶区 = 在该位置置顶，把置顶条目拖出置顶区末尾 = 取消置顶。最近区仍按活跃时间自动排序。

   验证方式：① 打开任一会话——顶栏重命名旁出现星形按钮；点击后星星点亮、该会话进入侧栏顶部置顶区（已有置顶时排在它们后面）；再点一次取消置顶。② 右键一个已置顶的会话/项目：菜单出现「上移 / 下移」（在首位时无「上移」、末位时无「下移」），点击后置顶区内顺序改变且重启后保留；未置顶项不显示这两项。③ 拖动已置顶的会话到置顶区其他位置——出现指示线，松手换序；把未置顶会话拖进置顶区 = 在该位置置顶；把置顶会话拖到最后一个置顶项之下（最近区）= 取消置顶。

## v0.4.17（2026-09-08）

1. **默认头像换图**：v0.4.16 内置的两张角色图替换为新的——智能体=哆啦A梦（戴竹蜻蜓飞行大笑），用户=大雄（躺平双手枕头）。仍是随包内置，设置 → 通用设置 → 「头像」可上传自定义覆盖。

   验证方式：发一条消息——自己消息右侧头像是躺平的大雄、智能体回复左侧是戴竹蜻蜓的哆啦A梦；设置页「头像」两个预览格同步更新。

2. **应用图标换为 WJW 竹蜻蜓设计**：深蓝 π 图标替换为浅蓝底的 W-J-W 竹蜻蜓（绿/黄双 W + J 形竹竿 + MPI 圆标 + 云朵），已去除模板水印、圆角外透明。任务栏、安装程序、标题栏品牌图标全部更新；生成流程固化在 `scripts/generate-icon.py`（源图 `resources/0.jpg`，重跑可再生成 png/ico/icns）。

   验证方式：看任务栏/开始菜单的 MPI 图标是否为浅蓝底竹蜻蜓；应用内标题栏左上角品牌小图标同步更新；安装程序窗口图标也是新的。

## v0.4.16（2026-09-08）

1. **默认头像改为哆啦A梦 / 大雄**：聊天里的内置默认头像不再是 🧑 emoji 和应用图标——用户消息默认显示「大雄」（红衣蓝裤），MPI 智能体回复默认显示「哆啦A梦」（大笑挥手）。两张图随安装包内置，无需联网加载；设置 → 通用设置 → 「头像」仍可上传自定义图片覆盖，点「恢复默认」回到哆啦A梦 / 大雄。

   验证方式：随便发一条消息——自己消息右侧的头像应是大雄、智能体回复左侧是哆啦A梦；打开设置 → 通用设置 → 「头像」一行，两个预览格分别显示大雄和哆啦A梦。上传自定义图片后覆盖生效，点「恢复默认」又回到角色图。

## v0.4.15（2026-09-08）

1. **自定义头像**：通用设置新增「头像」一行，可分别为「用户」和「MPI 智能体」上传自定义图片作为聊天消息左侧的头像。上传的图片会在本地自动压缩到 ≤192px 后再存入应用配置（不上传服务器），可随时「恢复默认」（用户=🧑、智能体=应用图标）。

   验证方式：设置 → 通用设置 → 「头像」一行有两个预览格；点「更换」选一张图片，预览立即更新，关闭设置后聊天里用户消息右侧、智能体回复左侧的头像都变成所选图片；再点「恢复默认」回到内置图标。重启应用后自定义头像仍然保留。
2. **权限模式扩展 + 新建对话默认权限**：在原有「沙盒 / 完全权限」基础上新增两档——「严格」（只读操作自动执行，所有写入/删除及修改类命令都需确认；对 write/edit 工具可选「本线程允许该工具」避免反复弹窗）和「只读」（只读操作可执行、任何修改直接阻止且不弹确认框，适合纯问答/分析会话）。通用设置新增「新建对话默认权限」下拉（只读/严格/沙盒/完全），控制之后新建对话的初始级别；已有会话保留各自设置。输入框左侧的权限菜单同步为四档，切换即时生效、无需重启会话。

   验证方式：① 设置 → 通用设置 → 「新建对话默认权限」有四个选项；选「严格」后新建一个对话，让 agent 修改某个文件——应弹出确认框（含「本线程允许工具」选项），而 `ls`、`git status` 等只读命令直接执行。② 在输入框左侧权限菜单切到「只读」，再让它写文件——操作被直接阻止、不弹确认框，但提问/读代码仍正常。③ 切回「沙盒」后低风险项目内操作（如 `npm run build`）恢复自动执行。
3. **对话窗口体验优化**：① 输入框不再限制在 860px——横向拉伸窗口时底部输入框跟随变宽（消息列表仍保持 860px 居中以保证阅读宽度）。② 输出跟随与完成定位修复：流式输出期间只要用户没有主动向上滚动，即使单帧内容增长超过阈值也会持续自动贴底（之前快速输出会「掉队」）；每轮会话结束（streaming 停止）时平滑滚动回最新消息底部。

   验证方式：① 把窗口拉得很宽，输入框应跟着变宽而不是停在中间一段。② 发一个会产生长回复的提示并保持在底部——输出全程自动跟随不卡顿；中途手动向上滚一点阅读历史则停止跟随（回到接近底部后恢复），该轮结束后会自动平滑滚回最下方。
4. **对话节点圆点导航显示门槛降低**：聊天区左侧的用户消息圆点导航原来要 6 条用户消息才出现，短对话里看不到、长对话突然出现，行为不一致。现改为 2 条用户消息即显示（1 条时没有跳转目标）。

   验证方式：新对话发第 2 条消息后，聊天区左侧中部应出现竖排圆点导航；悬停圆点显示该条消息预览，点击跳转到对应位置。
5. **开机自启动设置 + 头像行置顶**：通用设置新增「开机自启动」勾选行（Electron 登录项实现，Windows 下在开始菜单启动文件夹创建快捷方式；状态从系统实时读取，不占 config.json）。dev 实例的启动项会带上应用目录参数保证能真正拉起 MPI Dev。同时把「头像」一行移到通用设置最前面。

   验证方式：设置 → 通用设置 → 第一行是「头像」、第二行是「开机自启动」。勾选后按 Win+R 输入 shell:startup 回车，应看到新增的快捷方式（dev 实例名为 MPI Dev）；取消勾选则移除。实际自动启动在下次登录系统时生效。

## v0.4.14（2026-09-08）

1. **主题色预设重做为自定义色板**：按用户指定改为 7 个选项——「跟随主题」（默认，透明空圆圈，保持应用原有明暗外观）+ 白 / 浅灰 / 深灰 / 绿 / 红 / 蓝（中性色取 Apple systemGray 系取值，绿/红/蓝用 Apple HIG 系统色；明暗两套主题各有独立取值）。发送按钮及主操作按钮新增 `--send-fg`/`--send-ring` 变量：白色/浅灰预设下按钮为浅色底 + 深色图标并带细描边，保证可读性。旧预设（紫/粉/橙/薄荷/天蓝/薰衣草）移除，已选这些颜色的配置自动回落到「跟随主题」。

   验证方式：设置 → 通用设置 → 主题色一行应有 7 个圆圈：第一个是透明空圈（选中时下方显示「跟随主题」），其余为白、浅灰、深灰、绿、红、蓝；点白色后发送按钮变白底深色图标，点红色/蓝色后发送按钮与侧栏选中高亮相应变色。

## v0.4.13（2026-09-08）

1. **新增「通用设置」页**：设置侧栏顶部新增「通用设置」标签（打开设置的默认页），主题模式、语言、Diff 显示从侧栏底部移入此页，统一为「标题 + 控件 + 说明」的行布局——后续快捷键等其它全局设置直接在此追加行即可。

   验证方式：打开设置，左侧第一项应为「通用设置」且默认选中；页面内有主题/语言/Diff 显示三行，改动即时生效；侧栏底部不再有这三个下拉框（只剩一行共享说明）。
2. **主题模式可视化选择 + 苹果风格主题色**：主题从下拉改为「跟随系统」勾选框 + 浅色/深色两张预览卡片（纯 CSS 迷你 UI mockup，点击直接切换，选中态 accent 描边 + radio 指示）。新增 5 个主题色预设，采用 Apple HIG 官方系统色板（绿 #34C759 / 蓝 #007AFF / 紫 #AF52DE / 粉 #FF2D55 / 橙 #FF9500，暗色模式用对应深色取值）；选中态全面跟随主题色——侧栏线程、设置 Tab、斜杠菜单/搜索等列表项的选中背景改为 accent-soft 浅染（对齐飞书），发送按钮在明暗两主题下都跟随主题色；新 profile 也会从兄弟 profile 继承该设置。

   验证方式：设置 → 通用设置——「主题模式」下应有「跟随系统」勾选框和两张预览卡，点击即时切换、选中卡有强调色描边；「主题色」一行 5 个苹果官方色板，点蓝色后发送按钮变蓝、侧栏选中的会话/当前设置 Tab 背景变浅蓝。
3. **窗口缩放**：通用设置新增「窗口缩放」（50%–150%，默认 100%），快捷键 Ctrl+= / Ctrl+- 按 25% 步进调整、Ctrl+0 恢复默认；设置持久化，重启后自动恢复。

   验证方式：设置 → 通用设置 → 窗口缩放下拉选 125%，整个窗口内容放大；再按一次 Ctrl+= 到 150%、Ctrl+0 回 100%；重启应用缩放级别保持。
4. **主题色预设扩充——「默认」+ 浅色调**：主题色一行新增「默认」选项（恢复应用最初的柔和外观：暗绿强调 + 中性发送按钮）并作为新用户默认值；另加 3 个浅色调预设——薄荷 / 天蓝 / 薰衣草紫，明暗两主题各有独立取值。原苹果绿改为显式预设，已选过的用户不受影响。

   验证方式：设置 → 通用设置 → 主题色一行应有 9 个色板（默认 + 5 个鲜色 + 3 个浅色调）；点「默认」后发送按钮回到中性深灰（浅色主题）/柔和绿（深色主题）、选中高亮为暗绿；点薄荷/天蓝/薰衣草紫为更柔和的浅色调。

## v0.4.12（2026-09-08）

1. **新 profile 首次启动继承兄弟 profile 的语言/主题**：dev（`%APPDATA%\MPI Dev`）与 prod（`%APPDATA%\MPI`）配置目录相互独立，全新 profile 没有 config.json 时直接落默认值（英文 + light），这就是「重装后中文变英文」的成因。现在自己的目录没有 config.json（或文件损坏）时，会从另一个已知的 MPI profile 继承 `language` 与 `theme`；只继承这两项外观设置，置顶项目/会话、自动化任务等仍各自独立。测试：`npm run test:config`。

   验证方式：把 `%APPDATA%\MPI Dev\config.json`（或 `%APPDATA%\MPI\config.json`）改名备份后启动对应构建，界面语言与主题应与另一个 profile 一致而非默认英文；改回原名后以自己的配置为准。只装 prod 版的用户无感知（行为不变）。
2. **发送失败不再丢输入 / 留幽灵消息**：之前发送在任一环节失败（pi 进程连接失败、prompt RPC 抛错等）时，已清空的输入文本直接丢失；且 prompt RPC 本身报错时用户气泡会永久留在对话里成为无响应的「幽灵消息」。现在任何发送失败都会：① 回滚乐观气泡；② 把原文本/图片/附件恢复回输入框（含草稿持久化），可直接重发；③ 弹错误 toast，普通发送另在会话内显示 ⚠ 提示行。流式中的 steer/followUp 失败不再误重置进行中的任务状态；发送成功后侧栏刷新异常也不再被误报为「发送失败」。`/compact` 连接失败同样恢复输入。

   验证方式：难以日常主动复现（需 pi spawn/RPC 真实失败）；可临时把捆绑 runtime 目录改名后发一条消息——输入文本应回到输入框、会话不留幽灵气泡、出现错误 toast，恢复环境后可直接重发。

## v0.4.11（2026-09-08）

1. **changelog 验证行格式调整**：各条目原来的「验证」改为独立换行的「验证方式」，描述与操作步骤分行显示，便于安装后逐条对照实测。

   验证方式：设置 → 应用更新 → 查看更新日志，每条改动下方应有一行独立的「验证方式：」。

## v0.4.10（2026-09-08）

1. **edit 工具卡片自动展开直接显示 diff**：之前工具卡默认折叠，要先点细标题条再找「参数」区才能看到 unified diff，很容易错过（消息里更醒目的是文件产物 chip，点开是预览面板）。现在带可识别前后内容的 edit 卡片在参数就绪后自动展开——流式过程中参数补全即展开；超大编辑（合计 >240 行）保持折叠避免刷屏历史；手动收起过之后不再强制打开。多处替换的卡片折叠时标题条显示「N 处替换」徽标。

   验证方式：让 agent 对某文件做局部修改（如「只改第 X 行」，确保走 edit 工具而非 write），聊天里的工具卡应默认展开、直接看到单栏 -/+ diff，无需点击；手动收起后不再自动打开，多处替换时标题条出现「N 处替换」徽标。

## v0.4.9（2026-09-08）

1. **修复 edit 工具调用不显示 unified diff**：v0.4.7 的 diff 渲染只认扁平顶层 `oldText/newText` 参数，而 pi 实际 edit 工具用的是 `edits: [{oldText,newText}]` 数组结构（每次替换一条），从未匹配上、一直回退成原始 JSON——所以「参数」区看不到单栏 -/+ 视图。现在两种参数结构都支持；一次调用含多处替换时逐段渲染独立 diff，带「编辑 N/M」标签；前后分块模式同样按段显示。参数解析抽到 `lib/tool-args.ts`，新增测试 `npm run test:toolargs`（并用真实会话里 24 个 edit 调用验证过）。

   验证方式：让 agent 对某文件做局部修改（edit 工具），展开该工具卡「参数」区应显示单栏 -/+ diff；若仍是原始 JSON 则说明未装上本版本。

## v0.4.8（2026-09-08）

1. **输入框换行支持与中文输入法修复**：输入框明确支持 Shift+回车换行（占位提示同步标注）；修复中文/日文输入法组词期间按回车确认候选词会直接发送消息的问题——组词中所有按键交还输入法处理，不会误发送或误选斜杠命令；斜杠命令菜单打开时 Shift+回车也照常插入换行（不再被劫持为选择命令），换行后菜单自动收起。

   验证方式：输入框按 Shift+回车应插入换行且不发送；中文输入法打拼音组词中按回车确认候选词，消息不应被发出；输入 `/` 弹出斜杠菜单时按 Shift+回车仍插入换行、菜单收起。
2. **上下文面板显示压缩次数与建议**：上下文用量弹窗新增「已压缩 N 次」一行——直接读取会话 JSONL 中的 compaction 记录，重启不重置、未连接线程也可用，悬停显示最近一次压缩时间；下方按占用档位给出建议条（随进度条同色）：≤60% 暂无需压缩 / 60–74% 长任务可提前压缩 / 75–89% 建议手动压缩留出空间 / ≥90% 强烈建议立即压缩；会话累计压缩 ≥3 次时追加提醒「早期细节可能丢失，重要结论建议写入文件或记忆」。测试：`npm run test:compactions`。

   验证方式：点聊天区顶部的上下文用量按钮（显示 token 占用的那个），弹窗应出现「已压缩 N 次」一行和一条着色的建议条；悬停该行显示最近一次压缩时间。N>0 需要会话真的压缩过（发 `/compact` 或长对话触发自动压缩）。
3. **更新面板「查看更新日志」**：设置 → 应用更新的「MPI 应用更新」卡片新增 changelog 入口，点开为应用内更新日志（随包内置 CHANGELOG.md，无需联网），底部附「安装校验方法」：可信下载渠道、SHA256 校验步骤（`Get-FileHash` 对比同名 `.exe.sha256` 文件）、代码签名现状说明。打包流水线现在自动在每个安装包旁生成 `MPI-X.Y.Z.exe.sha256`（release/ 内历史安装包已补算）。

   验证方式：设置 → 应用更新卡片点「查看更新日志」，应弹出随包 changelog（内容即本文件），底部有 SHA256 校验步骤；Esc 或点遮罩关闭。

## v0.4.7（2026-09-08）

1. **GFM autolink 吞 CJK 文本修复**：`读https://example.com，然后来解决一下` 这类「URL 后紧跟中文」的文本，remark-gfm 的 autolink literal 会把后续所有非 ASCII 字符吞进链接（点击跳到百分号编码的无效 URL）。新增 `remark-autolink-trim` 插件在 gfm 之后把首个非 ASCII 字符起的尾部拆回普通文本；显式 `[文本](url)` 链接与纯 ASCII/百分号编码 URL 不受影响。测试：`npm run test:markdown`。
2. **Composer 草稿持久化**：未发送的输入（文本、粘贴图片、附件文件、HTML 元素引用）现按线程落盘保存，应用重启/崩溃后重新打开对应会话或项目的新任务时自动恢复；发送、排队后续消息或删除最后一个字符时清除。主进程以 LRU 语义保留最近使用的 40 份草稿（更新已存在条目时先删后插，避免 FIFO 截断误丢活跃草稿），单条超 2MB 时优先丢弃 base64 图片再落盘；文件存于应用配置目录 `drafts.json`（dev/prod 各自独立）。切换任务文件夹（composer 项目下拉或聊天顶栏按钮）时未发送文本跟随迁移，目标已有草稿则两者都保留。
3. **edit 工具结果 unified diff 视图**：编辑类工具（edit/patch/replace/update）的参数展示从「原内容/新内容」两个全文块改为 git 风格的单栏 unified 视图——上下文行 + `-`/`+` 着色行，带新旧两侧行号，一眼看出改了什么；设置侧栏新增「Diff 显示」开关（统一/前后分块），默认统一。输入过大超出 LCS 表上限时自动回退分块视图。测试：`npm run test:diff`。

## v0.4.6（2026-09-08）

1. **跳转到最新对话按钮**：向上滚动离开底部后，聊天区右下角出现圆形双箭头浮动按钮，点击平滑滚回最新消息；显示阈值与自动跟随一致（距底 140px）。

## v0.4.5（2026-09-08）

### 新功能

1. **流式输出自动滚动**：长回复在流式输出期间持续跟随贴底（修复单 block 内文本增长时停止滚动的 bug）；手动上滚阅读时不被拉回，滚回底部附近才恢复跟随。
2. **上下文用量变色提示**：按占用比例分档着色——≤60% 绿 / 60–74% 黄 / 75–89% 橙 / ≥90% 红；大数字、百分比标签与进度条同步变色（明暗主题各一套色值），一眼看出是否该压缩。
3. **砖会话一键修复**：当 provider 因「空白 tool result」或「孤儿 tool_result」（上游 #8720/#8667，无上游 PR）拒绝整个会话历史时，输入框上方出现红色横幅与「一键修复并重新加载」按钮。主进程对会话 JSONL 做手术（空白输出替换为占位文本、删除落在 tool call/result 对中间的过期压缩记录并重挂子节点），修改前自动备份到 `sessions/mpi-repair-backups/`，原子写回且幂等；修复后线程自动重开加载新上下文。

### 修复 / 改进

4. **优雅停机**：关闭线程或退出应用时若有进行中的 turn/压缩，先发 abort、等待 settle（上限 4s）再终止 pi 进程——避免硬杀留下无结果的 tool call 损坏会话文件（缓解上游 #9124，其 PR #9126 尚未合并）。空闲时零延迟；退出路径另有 8s 硬顶兜底。
5. **上下文占用比例精度**：pi 返回的 percent 为原始浮点数，现四舍五入到小数点后两位显示。

## v0.4.4（2026-09-08）

### 修复

1. **`/compact` 等内置指令报错**：TUI 内置命令在 RPC 模式下无法经 prompt 执行，现由 MPI 侧拦截并路由到对应能力（如 `/compact [instructions]` → 手动压缩）。
2. **扩展命令（如 `/mem0-status`）无反应且卡死会话**：扩展返回的 custom 消息现渲染为居中系统卡片；乐观流式锁增加宽限对账，不再永久挂起。

### 改进

3. **pi runtime 升级 0.84.1 → 0.85.1**（随包捆绑 `MPI-Runtime-0.85.1-win-x64`）。
4. **压缩失败提示改进**：自动压缩失败弹 warning toast；手动压缩区分「已取消」与「失败」并显示具体原因。
5. 修复 bundle-runtime 在 Node 24 上定位全局 pi 失败的问题（spawn `npm.cmd` 需 `shell: true`）。

## v0.4.3（2026-09-08）

### 新功能

1. **模型子面板「压缩上下文」按钮**：一键手动压缩，压缩中显示进度状态。
2. **完成提示音开关**：agent 运行完成时播放双音 ding（设置中可关闭）。

### 修复

3. **压缩后上下文用量显示为 0**：改用 `compaction_end` 的 `estimatedTokensAfter` 估算值展示，直到下次回复拿到真实 token 数。
