# Pi Studio 新手使用指南

适用版本：Pi Studio 0.3.1

这份指南带你完成一条最常用的流程：配置模型 -> 打开项目 -> 新建线程 -> 让 Pi 在项目中生成 Markdown 文件。

## 1. 先认识 Pi Studio

Pi Studio 是 Pi coding agent 的桌面界面。你可以把它理解为三个区域：

- 左侧栏：项目、线程和当前线程项目中的文件。
- 中间聊天区：输入任务、查看 Pi 的回复、选择模型和思考档位。
- 右侧预览区：预览 Markdown、HTML、代码、图片以及部分办公文件。

左侧栏顶部的“新建任务”会创建一个新线程。底部的设置图标打开“设置”。底部的额度图标可以查看 Pi 的累计 token 用量。

## 2. 配置模型

### 2.1 打开模型设置

1. 点击左下角的齿轮图标，打开“设置”。
2. 进入“模型与提供商”页。
3. 点击“添加提供商”。

Pi Studio 会把同一个提供商下的多个模型放在一起。配置完成并保存后，模型会出现在聊天输入框的模型选择器中。

### 2.2 添加一个提供商

在“添加模型提供商”表单中填写：

| 字段 | 填写说明 |
| --- | --- |
| 提供商名称 | 自定义名称，例如 `deepseek`、`openai` 或 `my-provider`。 |
| API 类型 | 按服务商文档选择 `openai-completions`、`openai-responses`、`anthropic-messages` 或 `google-generative-ai`。 |
| API 地址 | 服务商提供的 Base URL，例如 `https://api.example.com/v1`。 |
| API Key | 可以填写密钥，也可以填写环境变量引用，例如 `$MY_API_KEY`。不要把真实密钥写进截图、提交记录或共享文档。 |
| 模型 ID | 服务商真正使用的模型 ID，例如 `model-id`。 |

填写后点击“添加提供商”，然后点击“保存模型配置”。

如果你不确定 API 类型或 Base URL，请以服务商的 API 文档为准。兼容 OpenAI 的服务通常使用 OpenAI 兼容类型，但不同网关的要求可能不同。

### 2.3 配置模型能力

添加提供商后，在它下面找到模型卡片。模型卡片支持以下常用设置：

- “思考”：打开后，Pi Studio 会向该模型请求思考档位。
- “图像”：模型支持图片输入时勾选。
- 上下文长度：填写模型支持的上下文 token 数。
- 最大输出：填写模型允许的最大输出 token 数。
- “测试可用性”：检查 Base URL、API Key、模型 ID 是否可以正常调用。

如果模型支持思考，打开模型卡片右侧的高级设置。这里可以配置：

- `compat`：只有在服务商有特殊兼容要求时才修改。它是高级 JSON 字段，必须保持合法 JSON。
- `thinkingLevelMap`：按照每个 Pi 思考档位逐项选择服务商档位，不需要手写 JSON。

### 2.4 配置 thinkingLevelMap

Pi Studio 的标准思考档位是：

`off`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max`

在 `thinkingLevelMap` 中，每个 Pi 档位可以选择：

- “未指定”：使用服务商默认值，不写入该档位。
- “隐藏该档位”：在模型选择器中隐藏该档位，并写入 `null`。
- 一个标准档位：把 Pi 档位映射到服务商的同名或其他标准档位。
- “自定义提供方值”：填写服务商使用的非标准名称。

例如，某组模型需要使用以下映射时，可以按表格逐项配置：

| Pi 请求档位 | `deepseek-v4-flash` 的服务商档位 | `deepseek-v4-pro` 的服务商档位 |
| --- | --- | --- |
| `low` | `low` | `high` |
| `high` | `high` | `high` |
| `xhigh` | `high` | `max` |
| `max` | `max` | `max` |

注意：思考档位最终还会受到模型实时能力列表影响。如果服务商或模型没有声明某个档位，即使默认设置里存在 `max`，当前线程的模型选择器也可能不显示它。

### 2.5 设置默认模型和默认思考深度

1. 在设置中进入“思考默认值”。
2. 选择“默认思考深度”。
3. 选择“默认提供商”和“默认模型”。
4. 如果需要隐藏回复中的思考块，可以打开“隐藏思考块”。
5. 点击“保存思考默认值”。

这些是新线程的默认值。进入某个线程后，仍然可以在输入框右侧的模型按钮中临时切换模型和思考档位。

### 2.6 配置不生效时检查

可以打开“设置”中的“诊断与配置”，查看配置目录、`models.json`、`settings.json` 和 `auth.json` 的实际位置。Pi Studio 与终端 Pi 共享 `~/.pi/agent` 下的配置。

常见原因：

1. 模型 ID 拼写错误。
2. Base URL 多写或少写了路径，例如 `/v1`。
3. API 类型与服务商协议不匹配。
4. API Key 没有权限调用该模型。
5. 模型没有打开“思考”，所以不会出现思考档位。

## 3. 新建项目

Pi Studio 中的“项目”就是一个本地文件夹。Pi 会在这个文件夹中读取代码、创建文件并执行项目相关操作。

### 方法 A：从左侧栏打开项目

1. 在左侧“项目”标题旁点击 `+`。
2. 在系统文件夹选择窗口中选择一个已有文件夹。
3. 如果要从零开始，可以先在文件夹选择窗口中新建一个文件夹，再选择它。
4. 选中的文件夹会出现在左侧项目列表中，并成为当前项目。

### 方法 B：新建任务时选择项目

如果当前还没有项目，点击左侧的“新建任务”，Pi Studio 会先要求你选择项目文件夹。选择完成后，应用会自动打开一个新线程。

项目列表中的 `+` 可以直接在指定项目下新建线程。项目名称旁的数量表示该项目下已发现的线程数量。

### 项目与文件的关系

左侧“文件”页显示当前线程所在项目的文件。切换到另一个项目的线程后，文件树也会跟随当前线程更新。生成 Markdown 后，通常可以在这里看到新文件。

## 4. 新建线程并开始工作

### 4.1 创建线程

推荐使用以下任一入口：

- 点击左侧顶部的“新建任务”。
- 点击某个项目行右侧的 `+`。

新线程打开后，先不要急着发送消息。输入框上方或项目栏会显示当前工作文件夹。对于尚未发送第一条消息的草稿线程，可以点击项目名称并切换到另一个项目。

### 4.2 选择模型和思考档位

1. 点击输入框右下角的模型按钮。
2. 先点击提供商名称展开该提供商的模型列表。
3. 点击具体模型。
4. 在下面的“思考等级”区域选择 `off`、`low`、`high`、`max` 等档位。

模型列表按提供商分组，默认折叠。当前选中的模型和思考档位会显示在输入框右侧。

### 4.3 编写并发送任务

在输入框中写清楚四件事：

1. 你想完成什么。
2. 目标文件或目录在哪里。
3. 输出格式和限制是什么。
4. 完成后需要验证什么。

例如：

```text
请先查看当前项目结构，然后为这个项目写一份面向新手的 README。
要求：使用中文 Markdown，包含安装、启动、常用命令和故障排查。
请实际写入 README.md，完成后告诉我修改了哪些文件。
```

点击发送按钮，或按 Enter 发送。Pi 会在当前项目文件夹中工作，并将过程和结果显示在聊天区。

### 4.4 线程中的常用操作

- 双击聊天标题或点击标题旁的编辑按钮，可以重命名线程。
- 聊天头部的文件夹按钮可以切换工作文件夹。
- 聊天头部的刷新按钮是“新会话”，用于在当前工作上下文中开始一个新的会话。
- 聊天头部的预览按钮可以打开或关闭右侧预览区。
- 输入框左侧的 `+` 可以附加文件或图片。

默认权限为 `sandbox`。当任务需要执行敏感命令或写入项目外位置时，Pi Studio 可能会要求确认。新手建议先检查命令和目标路径，再决定是否允许。

## 5. 输出 Markdown 文件

Pi Studio 推荐的 Markdown 输出方式是：让 Pi 使用文件工具在当前项目中创建或更新 `.md` 文件，而不是只在聊天里返回一段代码块。

### 5.1 创建 Markdown 文件的提示词模板

```text
请在当前项目根目录下创建 docs/project-overview.md。

要求：
1. 使用标准 Markdown。
2. 包含项目简介、目录结构、安装步骤、启动方式和常见问题。
3. 标题层级清晰，命令使用代码块。
4. 如果 docs 目录不存在，请先创建它。
5. 请实际写入文件，不要只把内容贴在聊天中。
6. 完成后告诉我文件的相对路径，并检查文件是否存在。
```

如果要更新已有文件，可以这样说：

```text
请读取当前项目的 README.md，在不删除已有有效内容的前提下，补充“配置模型”和“输出 Markdown”两个章节，并直接保存回 README.md。
```

### 5.2 查看生成的文件

文件成功写入后，聊天消息下方会出现“文件产物”区域：

1. 点击文件产物，可以在 Pi Studio 的预览区打开它。
2. 也可以切换左侧的“文件”页，找到 `.md` 文件并点击。
3. 右侧预览区会按 Markdown 渲染标题、列表、表格、代码块和图片。
4. 点击预览区的展开按钮，可以让预览占据主要工作区；再次点击可以恢复聊天布局。
5. 右键文件可以打开文件相关菜单，在系统文件管理器中定位文件。

“复制”按钮只会复制聊天消息或代码块，不会自动生成文件。如果聊天中出现了 Markdown 内容，但没有出现文件产物，请继续发送：

```text
请把刚才的内容实际写入当前项目的 docs/result.md，并在完成后返回文件路径。
```

### 5.3 Markdown 文件的推荐检查清单

生成后建议检查：

- 文件是否位于当前项目目录，而不是临时目录。
- 文件名是否以 `.md` 结尾。
- 标题是否从 `#` 开始并按层级递进。
- 命令、代码和配置是否使用代码块。
- 相对链接和图片路径是否正确。
- 关键步骤是否可以由另一个新手照着完成。

## 6. 常见问题

### 模型选择器显示“无可用模型”

回到“设置 -> 模型与提供商”，检查模型是否保存。再检查 API Key、Base URL、API 类型和模型 ID。保存后重新打开模型选择器；必要时重新打开线程。

### 思考档位里没有 `max`

确认模型卡片已经打开“思考”，并检查 `thinkingLevelMap` 是否把 `max` 隐藏了。即使设置中可见 `max`，当前模型实时返回的能力列表也可能不支持它。

### 新线程没有出现在项目下

线程通常会在第一条消息发送后持久化。先发送一条任务，等待 Pi 完成或开始回复，再查看左侧项目下的线程列表。

### 文件页显示的不是当前项目

先切换到当前线程，再点击左侧“文件”页。文件树跟随当前线程的项目文件夹，而不是跟随上一个打开的项目。

### 生成了文件但预览没有更新

先确认聊天中的“文件产物”路径与当前项目一致，再关闭并重新打开预览。如果文件是由外部程序修改的，重新打开项目或重启 Pi Studio 后再查看。

## 7. 一套可以直接复制的首次使用流程

```text
1. 打开设置，添加模型提供商并点击“测试可用性”。
2. 保存模型配置，并在“思考默认值”中选择默认模型。
3. 在左侧打开一个项目文件夹。
4. 点击“新建任务”。
5. 在输入框右侧选择模型和思考档位。
6. 发送：

   请分析当前项目，并生成 docs/getting-started.md。
   内容面向第一次接触本项目的新手，包含项目用途、安装、启动、目录结构、
   常用命令和故障排查。请实际写入文件，完成后告诉我相对路径。

7. 点击聊天中的文件产物，检查 Markdown 预览。
```

完成这套流程后，你就可以把 Pi Studio 当作“项目文件夹 + AI 线程 + 文件预览”的工作台使用。
